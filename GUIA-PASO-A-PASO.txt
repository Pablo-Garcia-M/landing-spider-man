================================================================================
 CÓMO CONSTRUIR ESTA LANDING DE CERO, PASO A PASO
 (con el código completo de cada archivo, en el orden en que se escribe)
================================================================================

Este documento es distinto del README.md. El README está pensado para un
reclutador que quiere entender qué hay y cómo probarlo. Este TXT está pensado
para VOS: seguí los pasos en orden, con una carpeta vacía, y al final vas a
tener exactamente este proyecto — entendiendo por qué cada línea está donde
está, no sólo copiando y pegando.

Cada paso te dice: qué archivo crear, con qué contenido EXACTO, y por qué.
Cada tanto hay un "PUNTO DE CONTROL": parás, corrés un comando, y confirmás
que lo que hiciste hasta ahí funciona antes de seguir. Si algo no funciona,
es mucho más fácil encontrar el error en 20 líneas nuevas que en 3000.


================================================================================
 ÍNDICE
================================================================================
 PARTE 0  — Los 3 conceptos que explican todas las decisiones del proyecto
 PARTE 1  — Preparar la computadora (Node, npm)
 PARTE 2  — Crear el proyecto y agregar React
 PARTE 3  — package.json y astro.config.mjs
 PARTE 4  — La fuente única de datos: pelicula.js
 PARTE 5  — El sistema visual: fuentes.css y global.css
 PARTE 6  — El layout base: Base.astro
 PARTE 7  — Primer punto de control: una página mínima
 PARTE 8  — El Hero y sus dos primeras islas de React
 PARTE 9  — Atmósfera, Navegación y Sinopsis
 PARTE 10 — El tráiler (patrón facade)
 PARTE 11 — Reparto
 PARTE 12 — Sesiones (la isla de filtrado)
 PARTE 13 — Estreno y Compartir
 PARTE 14 — FAQ y Pie de página
 PARTE 15 — Ensamblar la página (index.astro) y la 404
 PARTE 16 — Archivos públicos: favicon, imagen para compartir, robots.txt
 PARTE 17 — Autoalojar las fuentes (en vez de depender de Google Fonts)
 PARTE 18 — Seguridad: Content-Security-Policy y cabeceras
 PARTE 19 — Cabeceras de caché para que aguante tráfico alto
 PARTE 20 — Pruebas automáticas de punta a punta
 PARTE 21 — .gitignore, antes de subirlo a GitHub
 PARTE 22 — Comandos que vas a usar siempre, y cómo desplegarlo


================================================================================
 PARTE 0 — LOS 3 CONCEPTOS QUE EXPLICAN TODAS LAS DECISIONES DEL PROYECTO
================================================================================

1) ESTÁTICO vs DINÁMICO
   -----------------------------------------------------------------------
   DINÁMICO = un servidor corriendo todo el tiempo, armando la página en el
   momento (consultando una base de datos, etc.).
   ESTÁTICO = se genera UNA VEZ un montón de archivos .html/.css/.js ya
   terminados, y después se reparten desde una CDN sin calcular nada.

   Esta landing es 100% estática. Por eso "aguanta picos de tráfico
   masivos" sin configurar nada raro: no hay nada que se pueda romper bajo
   carga, porque no hay ningún cálculo pasando por servidor en cada visita.

2) ARQUITECTURA DE ISLAS (lo que hace Astro)
   -----------------------------------------------------------------------
   HTML y CSS no necesitan JavaScript para existir. El JavaScript (React)
   sólo hace falta donde una parte de la página tiene que "recordar algo
   mientras el usuario interactúa" (estado). Astro te deja escribir TODA la
   página en HTML/CSS y meter React SÓLO en esos pedacitos puntuales — a
   esos pedacitos los llama "islas". En esta landing hay 4 islas:
     - el contador (recalcula cada segundo)
     - el botón de calendario (abre/cierra un menú)
     - el reproductor de tráiler (cambia de portada a video)
     - el buscador de sesiones (filtra sin recargar)
   Todo lo demás es HTML/CSS puro, cero JavaScript de framework.

3) SEPARAR DATOS DE DISEÑO
   -----------------------------------------------------------------------
   Un solo archivo (src/data/pelicula.js) con TODOS los datos que cambian
   (fecha, actores, preguntas frecuentes, cines). El resto del código LEE
   de ahí, nunca escribe la información a mano en el medio de un componente.


================================================================================
 PARTE 1 — PREPARAR LA COMPUTADORA
================================================================================

Necesitás Node.js (trae npm, el instalador de paquetes, incluido).

  Windows (con winget, ya viene en Windows 10/11):
    winget install --id OpenJS.NodeJS.LTS

  Confirmar que quedó instalado:
    node -v
    npm -v

Node ejecuta JavaScript FUERA del navegador, en tu computadora. Astro,
React y todas las herramientas de este proyecto están escritas en
JavaScript y necesitan Node para correr durante el desarrollo. El
visitante final de la página NUNCA ve Node: sólo recibe los archivos
.html/.css/.js ya generados por el build.


================================================================================
 PARTE 2 — CREAR EL PROYECTO Y AGREGAR REACT
================================================================================

Parado en la carpeta donde querés el proyecto:

  npm create astro@latest

Preguntas que te va a hacer (y qué contestar para llegar a este resultado):
  - Nombre de la carpeta: landing-spider-man
  - Plantilla: "Empty" (vacía, sin ejemplos de por medio)
  - TypeScript: NO. Este proyecto usa JavaScript puro a propósito — si no
    sabés TypeScript, mezclarlo obliga a aprender dos cosas a la vez.
  - Instalar dependencias: sí
  - Inicializar git: como prefieras (no es necesario para seguir esta guía)

Después, entrá a la carpeta y agregá React:

  cd landing-spider-man
  npx astro add react

Este comando hace TODO solo: instala @astrojs/react y edita
astro.config.mjs para activarlo. Es la forma recomendada de agregar
integraciones a Astro (mejor que instalar el paquete a mano y editar el
config vos mismo/a).

También vamos a necesitar la integración de sitemap (un mapa del sitio
para buscadores, Parte 16) y una librería sólo para las pruebas
automáticas (Parte 20):

  npx astro add sitemap
  npm install -D playwright-core


================================================================================
 PARTE 3 — package.json Y astro.config.mjs
================================================================================

Después de los comandos de la Parte 2, tu package.json va a quedar
parecido a esto (ajustalo si algo no coincide exactamente — lo importante
son los scripts y las dependencias, no el orden):

--------------------------------------------------------------------------
ARCHIVO: package.json
--------------------------------------------------------------------------
{
  "name": "landing-spider-man",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Landing de SPIDER-MAN: Brand New Day — Astro + React",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node tests/verificar.mjs"
  },
  "dependencies": {
    "@astrojs/react": "^6.0.1",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.3",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.0"
  }
}

Fijate los scripts: "test" no es un comando propio de Astro, lo agregamos
a mano para que "npm test" ejecute nuestro archivo de pruebas (Parte 20).

--------------------------------------------------------------------------
ARCHIVO: astro.config.mjs (contenido FINAL — le vamos a ir sumando cosas
en las partes 16 a 19; acá está completo para que lo tengas de referencia)
--------------------------------------------------------------------------
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import sitemap from '@astrojs/sitemap';

// https://astro.build/config
export default defineConfig({
  // Cambiá esto por el dominio real antes de desplegar: alimenta las URLs
  // canónicas, el sitemap y las etiquetas Open Graph absolutas que exige WhatsApp.
  site: 'https://spiderman-brand-new-day.example',

  integrations: [react(), sitemap()],

  // Salida 100% estática. Sin servidor que mantener, sin cold starts:
  // la landing se sirve desde CDN y aguanta un pico de tráfico masivo
  // sin escalar nada — que es exactamente lo que pide el brief.
  output: 'static',

  build: {
    // Astro decide por hoja: las chicas van inline (menos requests en el
    // camino crítico), las grandes quedan como archivo cacheable.
    inlineStylesheets: 'auto',
  },

  prefetch: {
    prefetchAll: true,
    defaultStrategy: 'viewport',
  },

  vite: {
    build: {
      // Menos ruido en el bundle: los assets chicos se vuelven data URI.
      assetsInlineLimit: 2048,
    },
  },

  security: {
    // CSP con hashes automáticos: Astro calcula el hash de cada script y
    // estilo que genera y arma la política él solo. No hace falta 'unsafe-
    // inline' (que anularía la protección) ni un servidor que emita un nonce
    // distinto por request, imposible en un sitio 100% estático.
    //
    // Se llamaba `experimental.csp` hasta Astro 6; se estabilizó como
    // `security.csp` en Astro 7.
    csp: {
      directives: [
        "default-src 'self'",
        "img-src 'self' data:",
        "font-src 'self'",
        // El tráiler se embebe desde youtube-nocookie recién al hacer click
        // (patrón facade); sin este permiso el iframe quedaría bloqueado.
        "frame-src https://www.youtube-nocookie.com",
        "object-src 'none'",
        "base-uri 'self'",
        "form-action 'self'",
      ],
    },
  },
});

Nota: si vas construyendo paso a paso, podés arrancar SIN el bloque
"security" y agregarlo recién en la Parte 18 — el sitio funciona igual sin
él, sólo que sin esa capa extra de seguridad.


================================================================================
 PARTE 4 — LA FUENTE ÚNICA DE DATOS: pelicula.js
================================================================================

Creá la carpeta src/data/ y, adentro, el archivo pelicula.js. Este es el
PRIMER archivo de contenido real que escribimos, porque todo lo demás va a
importar de acá.

--------------------------------------------------------------------------
ARCHIVO: src/data/pelicula.js
--------------------------------------------------------------------------
/**
 * Fuente única de verdad de la landing.
 *
 * Todo dato que aparezca en pantalla vive acá: si mañana cambia la fecha de
 * estreno o llega el ID del tráiler, se toca un archivo y no doce componentes.
 * Los componentes importan de acá y nunca hardcodean texto de negocio.
 */

export const PELICULA = {
  titulo: 'Spider-Man',
  subtitulo: 'Brand New Day',
  tituloCompleto: 'Spider-Man: Brand New Day',

  /**
   * Zona horaria explícita (+02:00 = Madrid en horario de verano).
   * Sin ella, cada visitante calcularía el countdown contra su propio huso
   * y vería un número distinto: el estreno es a una hora concreta, no
   * "medianoche donde estés".
   */
  estrenoISO: '2026-07-29T00:00:00+02:00',
  estrenoDia: '29',
  estrenoMes: 'Julio',
  estrenoAnio: '2026',
  estrenoLargo: '29 de julio de 2026',

  /** Formato compacto para el archivo .ics (YYYYMMDD, evento de día completo). */
  estrenoICS: { inicio: '20260729', fin: '20260730' },

  director: 'Destin Daniel Cretton',
  estudios: 'Marvel Studios · Sony Pictures',
  clasificacion: 'Pendiente de calificación',
  idiomas: 'Castellano · V.O.S.E.',

  /**
   * ID de YouTube del tráiler oficial. Mientras esté vacío, el componente
   * <Trailer /> muestra el estado "próximamente" en lugar de romper con un
   * embed a un vídeo inexistente. Cargá el ID y el reproductor se activa solo.
   */
  trailerId: '',

  descripcion:
    'Cuenta atrás para el estreno de SPIDER-MAN: Brand New Day. 29 de julio de 2026, solo en cines.',
};

/**
 * Reparto anunciado públicamente. Es una landing de fan/portfolio: los datos
 * provienen de anuncios de prensa y pueden cambiar hasta el estreno.
 */
export const REPARTO = [
  { actor: 'Tom Holland', personaje: 'Peter Parker / Spider-Man' },
  { actor: 'Zendaya', personaje: 'MJ' },
  { actor: 'Jacob Batalon', personaje: 'Ned Leeds' },
  { actor: 'Jon Bernthal', personaje: 'Frank Castle / The Punisher' },
  { actor: 'Mark Ruffalo', personaje: 'Bruce Banner / Hulk' },
  { actor: 'Sadie Sink', personaje: 'Personaje sin anunciar' },
  { actor: 'Michael Mando', personaje: 'Mac Gargan / Scorpion' },
  { actor: 'Liza Colón-Zayas', personaje: 'Personaje sin anunciar' },
];

/**
 * DATOS DE DEMOSTRACIÓN. En producción esto lo devuelve la API de la
 * distribuidora; el componente <Entradas /> ya está escrito contra esta forma,
 * así que sustituir el objeto por un `fetch` no cambia una línea de la vista.
 *
 * Forma de cada sesión:
 *   { cine: string, sala: string, horas: string[], formato: string }
 */
export const CINES = {
  Madrid: [
    { cine: 'Kinépolis Ciudad de la Imagen', sala: 'Sala 25', horas: ['16:30', '19:45', '22:50'], formato: 'IMAX' },
    { cine: 'Cines Callao', sala: 'Sala 1', horas: ['17:00', '20:10', '23:15'], formato: 'Digital' },
    { cine: 'Yelmo Ideal', sala: 'Sala 4', horas: ['18:20', '21:30'], formato: 'V.O.S.E.' },
  ],
  Barcelona: [
    { cine: 'Cinesa Diagonal Mar', sala: 'Sala 12', horas: ['16:00', '19:20', '22:30'], formato: 'IMAX' },
    { cine: 'Aribau Multicines', sala: 'Sala 2', horas: ['17:45', '20:50'], formato: 'V.O.S.E.' },
    { cine: 'Yelmo Icaria', sala: 'Sala 9', horas: ['18:00', '21:10', '00:05'], formato: '4DX' },
  ],
  Valencia: [
    { cine: 'Kinépolis Valencia', sala: 'Sala 18', horas: ['16:15', '19:30', '22:40'], formato: 'IMAX' },
    { cine: 'Cines Lys', sala: 'Sala 3', horas: ['17:30', '20:40'], formato: 'V.O.S.E.' },
  ],
  Sevilla: [
    { cine: 'Nervión Plaza', sala: 'Sala 7', horas: ['16:45', '20:00', '23:00'], formato: 'Digital' },
    { cine: 'Cinesur Los Arcos', sala: 'Sala 5', horas: ['18:10', '21:20'], formato: 'Digital' },
  ],
  Bilbao: [
    { cine: 'Yelmo Megapark', sala: 'Sala 14', horas: ['17:15', '20:25', '23:10'], formato: '4DX' },
    { cine: 'Golem Alhóndiga', sala: 'Sala 1', horas: ['18:40', '21:45'], formato: 'V.O.S.E.' },
  ],
};

export const CIUDADES = Object.keys(CINES);

export const FAQ = [
  {
    pregunta: '¿Cuándo se estrena?',
    respuesta:
      'El 29 de julio de 2026 en cines de toda España. Los pases de medianoche del día 28 se anunciarán en las próximas semanas.',
  },
  {
    pregunta: '¿Hay escenas post-créditos?',
    respuesta: 'Es Marvel. Quedate sentado hasta que se encienda la luz de la sala.',
  },
  {
    pregunta: '¿Cuándo salen las entradas a la venta?',
    respuesta:
      'La venta anticipada abre unas semanas antes del estreno. Añadí el estreno a tu calendario desde esta página y no se te va a pasar.',
  },
  {
    pregunta: '¿Se puede ver en versión original?',
    respuesta:
      'Sí. Filtrá por tu ciudad en la sección de sesiones: las copias en V.O.S.E. están marcadas con su etiqueta.',
  },
  {
    pregunta: '¿Esta web es oficial?',
    respuesta:
      'No. Es un proyecto de portfolio construido con Astro y React. No está afiliado a Sony Pictures ni a Marvel; las marcas y personajes pertenecen a sus titulares.',
  },
];

/** Enlaces de la navegación. El id debe existir como ancla en la página. */
export const NAVEGACION = [
  { id: 'sinopsis', texto: 'Sinopsis' },
  { id: 'trailer', texto: 'Tráiler' },
  { id: 'reparto', texto: 'Reparto' },
  { id: 'sesiones', texto: 'Sesiones' },
  { id: 'faq', texto: 'FAQ' },
];

Fijate: es JavaScript común, "export const" para que otros archivos puedan
"import { PELICULA } from '../data/pelicula.js'". No hay nada de Astro ni
de React acá — por eso este mismo archivo podría usarlo también un backend
si algún día lo hubiera.


================================================================================
 PARTE 5 — EL SISTEMA VISUAL: fuentes.css Y global.css
================================================================================

Creá la carpeta src/styles/. Por ahora (vamos a volver en la Parte 17 para
autoalojar las fuentes de verdad), src/styles/fuentes.css puede quedar
vacío o con este comentario:

--------------------------------------------------------------------------
ARCHIVO: src/styles/fuentes.css (versión provisoria — la completamos en
la Parte 17)
--------------------------------------------------------------------------
/* Las reglas @font-face van acá. Ver Parte 17 de la guía. */

Y ahora el archivo grande: src/styles/global.css. Es el sistema visual
COMPLETO del sitio: colores, tamaños, botones, y también los estilos de
las 4 islas de React (más abajo se explica por qué viven acá y no en cada
componente).

--------------------------------------------------------------------------
ARCHIVO: src/styles/global.css
--------------------------------------------------------------------------
/* ============================================================================
   SPIDER-MAN: BRAND NEW DAY — sistema visual
   ----------------------------------------------------------------------------
   Este archivo contiene sólo lo compartido: tokens, reset, tipografía,
   utilidades y los primitivos (botón, sección, revelado). El estilo propio de
   cada componente .astro vive dentro del componente, con scope automático.
   Las islas de React sí se estilan desde acá: el scope de Astro no cruza el
   límite del framework, así que sus clases necesitan ser globales.
   ========================================================================== */

/* ----------------------------------------------------------------------------
   1. TOKENS
   Un amanecer sobre Nueva York, de la noche cerrada al primer naranja.
   -------------------------------------------------------------------------- */
:root {
  --noche: #0b1026;
  --medianoche: #131a3a;
  --cielo: #2b4c8c;
  --horizonte: #c41e3a;
  --alba: #ff6b35;

  --luz: #f2ede4;
  --luz-tenue: #8b90a8;

  /* Superficies translúcidas: siempre derivadas de --luz para que un cambio
     de paleta se propague sin buscar rgba() sueltos por el proyecto. */
  --velo-01: rgb(242 237 228 / 0.04);
  --velo-02: rgb(242 237 228 / 0.08);
  --borde: rgb(242 237 228 / 0.14);
  --borde-fuerte: rgb(242 237 228 / 0.28);

  --display: 'Archivo', system-ui, sans-serif;
  --texto: 'Manrope', system-ui, sans-serif;
  --mono: 'Space Mono', ui-monospace, monospace;

  --espacio: clamp(1rem, 3vw, 2rem);
  --ancho: min(72rem, 92vw);
  --ancho-nav: min(80rem, 94vw);

  --transicion: 220ms cubic-bezier(0.2, 0.8, 0.2, 1);
  --altura-nav: 4.25rem;
}

/* ----------------------------------------------------------------------------
   2. RESET Y BASE
   -------------------------------------------------------------------------- */
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
  /* El ancla no queda tapada por la barra fija. */
  scroll-padding-top: calc(var(--altura-nav) + 1rem);
  color-scheme: dark;
}

body {
  background: var(--noche);
  color: var(--luz);
  font-family: var(--texto);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
  overflow-x: hidden;
}

img,
svg,
iframe,
video {
  display: block;
  max-width: 100%;
}

button,
input,
select,
textarea {
  font: inherit;
  color: inherit;
}

:focus-visible {
  outline: 2px solid var(--alba);
  outline-offset: 4px;
  border-radius: 2px;
}

::selection {
  background: var(--alba);
  color: var(--noche);
}

/* Quien pidió menos movimiento recibe menos movimiento. No es un extra. */
@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* ----------------------------------------------------------------------------
   3. UTILIDADES
   -------------------------------------------------------------------------- */

/* Invisible en pantalla, presente para el lector de pantalla. */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip-path: inset(50%);
  white-space: nowrap;
  border: 0;
}

.saltar {
  position: absolute;
  top: -100%;
  left: var(--espacio);
  z-index: 200;
  padding: 0.75em 1.25em;
  background: var(--luz);
  color: var(--noche);
  font-weight: 800;
  text-decoration: none;
}
.saltar:focus {
  top: var(--espacio);
}

/* Revelado al entrar en pantalla. Lo activa un IntersectionObserver global. */
.revelar {
  opacity: 0;
  transform: translateY(28px);
  transition:
    opacity 700ms ease,
    transform 700ms cubic-bezier(0.2, 0.8, 0.2, 1);
}
.revelar.visible {
  opacity: 1;
  transform: none;
}
/* Escalonado: cada hijo entra un pelo después que el anterior. */
.revelar[data-retraso='1'] { transition-delay: 90ms; }
.revelar[data-retraso='2'] { transition-delay: 180ms; }
.revelar[data-retraso='3'] { transition-delay: 270ms; }
.revelar[data-retraso='4'] { transition-delay: 360ms; }

@media (prefers-reduced-motion: reduce) {
  .revelar {
    opacity: 1;
    transform: none;
  }
}

/* ----------------------------------------------------------------------------
   4. TIPOGRAFÍA DE MARCA
   Todo el tratamiento del display vive en el eje de ancho de Archivo:
   condensado y pesado para gritar, expandido y liviano para respirar.
   -------------------------------------------------------------------------- */
.eyebrow {
  font-family: var(--mono);
  font-size: clamp(0.65rem, 1.5vw, 0.75rem);
  letter-spacing: 0.28em;
  text-transform: uppercase;
  color: var(--alba);
}

.titulo-seccion {
  font-family: var(--display);
  font-variation-settings: 'wdth' 70, 'wght' 800;
  font-size: clamp(2.1rem, 7vw, 4.5rem);
  line-height: 0.95;
  text-transform: uppercase;
  letter-spacing: -0.005em;
}

.parrafo {
  font-size: clamp(1.02rem, 1.6vw, 1.2rem);
  color: var(--luz);
  opacity: 0.82;
  max-width: 58ch;
}

/* ----------------------------------------------------------------------------
   5. SECCIÓN
   -------------------------------------------------------------------------- */
.bloque {
  width: var(--ancho);
  margin-inline: auto;
  padding-block: clamp(4rem, 11vh, 8.5rem);
}

.bloque__cabecera {
  display: flex;
  flex-direction: column;
  gap: 1.1rem;
  margin-bottom: clamp(2rem, 5vw, 3.5rem);
}

/* ----------------------------------------------------------------------------
   6. BOTONES
   -------------------------------------------------------------------------- */
.boton {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.6em;
  padding: 0.9em 2em;
  font-family: var(--display);
  font-variation-settings: 'wdth' 85, 'wght' 700;
  font-size: clamp(0.78rem, 1.6vw, 0.95rem);
  text-transform: uppercase;
  letter-spacing: 0.16em;
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  border: 2px solid var(--luz);
  background: transparent;
  color: var(--luz);
  transition:
    background var(--transicion),
    color var(--transicion),
    border-color var(--transicion),
    transform var(--transicion);
}

.boton:hover,
.boton:focus-visible {
  background: var(--luz);
  color: var(--noche);
}

.boton:active {
  transform: translateY(1px);
}

.boton--solido {
  background: var(--luz);
  color: var(--noche);
}
.boton--solido:hover,
.boton--solido:focus-visible {
  background: var(--alba);
  border-color: var(--alba);
  color: var(--noche);
}

.boton--fantasma {
  border-color: var(--borde-fuerte);
}

.boton[disabled],
.boton[aria-disabled='true'] {
  opacity: 0.45;
  cursor: not-allowed;
}
.boton[disabled]:hover,
.boton[aria-disabled='true']:hover {
  background: transparent;
  color: var(--luz);
}

.acciones {
  display: flex;
  flex-wrap: wrap;
  gap: 0.85rem;
}

/* ============================================================================
   ISLAS DE REACT
   Estilos globales porque el scope de Astro no cruza a otro framework.
   Prefijo por componente para que sigan siendo legibles como un módulo.
   ========================================================================== */

/* ----------------------------------------------------------------------------
   R1. CUENTA ATRÁS
   Monoespaciada por motivo funcional, no estético: todos los dígitos miden
   igual, así que pasar de 09 a 10 no empuja el layout un pixel.
   -------------------------------------------------------------------------- */
.cuenta {
  display: flex;
  justify-content: center;
  align-items: flex-start;
  gap: clamp(0.7rem, 2.6vw, 1.9rem);
  margin-top: clamp(1.75rem, 4vw, 2.75rem);
}

.cuenta__unidad {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 3.4ch;
}

.cuenta__numero {
  font-family: var(--mono);
  font-variant-numeric: tabular-nums;
  font-size: clamp(1.7rem, 5.5vw, 3.4rem);
  line-height: 1;
  color: var(--luz);
}

.cuenta__label {
  font-weight: 600;
  font-size: clamp(0.53rem, 1.4vw, 0.68rem);
  letter-spacing: 0.2em;
  text-transform: uppercase;
  opacity: 0.65;
  margin-top: 0.75em;
}

.cuenta__sep {
  width: 1px;
  height: clamp(1.7rem, 5.5vw, 3.4rem);
  background: var(--luz);
  opacity: 0.25;
}

/* Estado post-estreno: el contador se retira y deja el mensaje. */
.cuenta--estreno {
  font-family: var(--display);
  font-variation-settings: 'wdth' 75, 'wght' 800;
  font-size: clamp(1.5rem, 5vw, 3rem);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--alba);
}

/* ----------------------------------------------------------------------------
   R2. TRÁILER (patrón facade)
   -------------------------------------------------------------------------- */
.trailer {
  position: relative;
  /* Reserva el hueco antes de cargar nada: cero layout shift, CLS a cero. */
  aspect-ratio: 16 / 9;
  width: 100%;
  overflow: hidden;
  border: 1px solid var(--borde);
  background:
    radial-gradient(75% 95% at 50% 108%, var(--horizonte) 0%, transparent 62%),
    linear-gradient(160deg, var(--medianoche), var(--noche));
}

.trailer__boton {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  align-content: center;
  gap: 1.1rem;
  width: 100%;
  border: 0;
  background: transparent;
  color: var(--luz);
  cursor: pointer;
  font-family: var(--display);
  font-variation-settings: 'wdth' 85, 'wght' 700;
  font-size: clamp(0.72rem, 1.6vw, 0.9rem);
  text-transform: uppercase;
  letter-spacing: 0.2em;
}

.trailer__boton[disabled] {
  cursor: default;
  opacity: 0.75;
}

.trailer__icono {
  width: clamp(56px, 8vw, 82px);
  height: clamp(56px, 8vw, 82px);
  border: 2px solid var(--luz);
  border-radius: 50%;
  display: grid;
  place-items: center;
  transition:
    background var(--transicion),
    border-color var(--transicion),
    transform var(--transicion);
}

.trailer__icono svg {
  width: 36%;
  fill: var(--luz);
  transition: fill var(--transicion);
}
.trailer__icono--play svg {
  margin-left: 12%;
}

.trailer__boton:not([disabled]):hover .trailer__icono,
.trailer__boton:focus-visible .trailer__icono {
  background: var(--alba);
  border-color: var(--alba);
  transform: scale(1.06);
}
.trailer__boton:not([disabled]):hover .trailer__icono svg {
  fill: var(--noche);
}

.trailer__nota {
  font-family: var(--texto);
  font-variation-settings: normal;
  font-size: 0.78rem;
  letter-spacing: 0.02em;
  text-transform: none;
  opacity: 0.6;
  max-width: 30ch;
  text-align: center;
}

.trailer iframe {
  width: 100%;
  height: 100%;
  border: 0;
}

/* Estado de repuesto cuando la isla de React falla en pleno render (ver
   ErrorBoundary.jsx). Clase en vez de style="" inline: bajo la CSP con
   hashes automáticos de Astro, un atributo style="" necesitaría
   'unsafe-inline' en style-src, que es justo lo que la CSP existe para evitar. */
.trailer--fallback {
  display: grid;
  place-items: center;
  padding: 2rem;
}

/* ----------------------------------------------------------------------------
   R3. BOTÓN DE CALENDARIO (menú)
   -------------------------------------------------------------------------- */
.cal {
  position: relative;
  display: inline-block;
}

.cal__menu {
  position: absolute;
  top: calc(100% + 0.5rem);
  left: 0;
  z-index: 50;
  min-width: 15rem;
  padding: 0.4rem;
  list-style: none;
  background: var(--medianoche);
  border: 1px solid var(--borde);
  box-shadow: 0 18px 50px rgb(0 0 0 / 0.55);
}

/* Cerca del borde derecho el menú se ancla al otro lado y no desborda. */
.cal--derecha .cal__menu {
  left: auto;
  right: 0;
}

.cal__opcion {
  display: flex;
  width: 100%;
  align-items: center;
  gap: 0.7rem;
  padding: 0.7rem 0.9rem;
  border: 0;
  background: transparent;
  color: var(--luz);
  font-size: 0.9rem;
  text-align: left;
  text-decoration: none;
  cursor: pointer;
  transition: background var(--transicion);
}
.cal__opcion:hover,
.cal__opcion:focus-visible {
  background: var(--velo-02);
}

.cal__icono {
  width: 1.05rem;
  height: 1.05rem;
  flex: none;
  fill: none;
  stroke: var(--alba);
  stroke-width: 1.6;
}

.cal__flecha {
  width: 0.7em;
  height: 0.7em;
  fill: currentColor;
  transition: transform var(--transicion);
}
.cal[data-abierto='true'] .cal__flecha {
  transform: rotate(180deg);
}

.cal__fallback {
  margin: 0;
}

.cal__aviso {
  position: absolute;
  top: calc(100% + 0.5rem);
  left: 0;
  white-space: nowrap;
  font-size: 0.78rem;
  color: var(--alba);
}

/* ----------------------------------------------------------------------------
   R4. SESIONES
   -------------------------------------------------------------------------- */
.sesiones__ciudades {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-bottom: 2rem;
}

.sesiones__ciudad {
  padding: 0.55em 1.3em;
  border: 1px solid var(--borde);
  background: transparent;
  color: var(--luz);
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.04em;
  cursor: pointer;
  transition:
    background var(--transicion),
    border-color var(--transicion),
    color var(--transicion);
}
.sesiones__ciudad:hover {
  border-color: var(--borde-fuerte);
  background: var(--velo-01);
}
.sesiones__ciudad[aria-selected='true'],
.sesiones__ciudad[data-activo='true'] {
  background: var(--luz);
  border-color: var(--luz);
  color: var(--noche);
}

.sesiones__lista {
  display: grid;
  gap: 1px;
  background: var(--borde);
  border: 1px solid var(--borde);
}

.sesion {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 1rem 1.5rem;
  align-items: center;
  padding: clamp(1.1rem, 2.5vw, 1.6rem);
  background: var(--noche);
}

@media (max-width: 44rem) {
  .sesion {
    grid-template-columns: 1fr;
  }
}

.sesion__cine {
  font-family: var(--display);
  font-variation-settings: 'wdth' 90, 'wght' 700;
  font-size: clamp(1rem, 2.2vw, 1.25rem);
  line-height: 1.2;
}

.sesion__meta {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 0.5rem 0.9rem;
  margin-top: 0.5rem;
  font-size: 0.8rem;
  color: var(--luz-tenue);
}

.sesion__formato {
  font-family: var(--mono);
  font-size: 0.65rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  padding: 0.25em 0.6em;
  border: 1px solid var(--borde-fuerte);
  color: var(--alba);
}

.sesion__horas {
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
}

.sesion__hora {
  font-family: var(--mono);
  font-variant-numeric: tabular-nums;
  font-size: 0.88rem;
  padding: 0.5em 0.9em;
  border: 1px solid var(--borde-fuerte);
  background: transparent;
  color: var(--luz);
  cursor: pointer;
  transition:
    background var(--transicion),
    color var(--transicion),
    border-color var(--transicion);
}
.sesion__hora:hover,
.sesion__hora:focus-visible {
  background: var(--alba);
  border-color: var(--alba);
  color: var(--noche);
}

.sesiones__aviso {
  margin-top: 1.25rem;
  font-size: 0.78rem;
  color: var(--luz-tenue);
}

--------------------------------------------------------------------------
Por qué los estilos de las islas de React viven ACÁ y no en cada .astro
--------------------------------------------------------------------------
Cada .astro puede tener su propio <style> y Astro lo aísla automáticamente
("scoped styles": una clase ".titulo" en un componente no choca con otra
igual en otro componente). Pero ese aislamiento es magia que SÓLO entiende
Astro. Si CuentaAtras.jsx (que es React) usa className="cuenta__numero",
Astro no puede "adivinar" que ese estilo le pertenece si estuviera escrito
dentro de, por ejemplo, Hero.astro. Por eso todos los estilos que usan las
4 islas viven juntos acá, sin scope, con prefijo de nombre (cuenta__,
trailer__, cal__, sesion__) para no chocar entre sí a mano.


================================================================================
 PARTE 6 — EL LAYOUT BASE: Base.astro
================================================================================

Creá la carpeta src/layouts/ y el archivo Base.astro. Es la "envoltura" de
toda página: el <head> con SEO, las fuentes, y el script de revelado al
scroll. Cualquier página que crees más adelante (index, 404) se envuelve
en esto.

--------------------------------------------------------------------------
ARCHIVO: src/layouts/Base.astro
--------------------------------------------------------------------------
---
import '../styles/fuentes.css';
import '../styles/global.css';
import { PELICULA } from '../data/pelicula.js';

// Props opcionales: { titulo, descripcion }. Si no llegan, se usan los
// valores por defecto de abajo, que es el caso de la home.
const {
  titulo = `${PELICULA.tituloCompleto} — ${PELICULA.estrenoLargo}, solo en cines`,
  descripcion = PELICULA.descripcion,
} = Astro.props;

// URL absoluta: las tarjetas de WhatsApp, X y Slack no resuelven rutas relativas.
const canonica = new URL(Astro.url.pathname, Astro.site);
const ogImagen = new URL('/og.png', Astro.site);
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <title>{titulo}</title>
    <meta name="description" content={descripcion} />
    <link rel="canonical" href={canonica} />
    <meta name="theme-color" content="#0B1026" />

    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="sitemap" href="/sitemap-index.xml" />

    {/* Cómo se ve el enlace cuando alguien lo comparte. */}
    <meta property="og:type" content="video.movie" />
    <meta property="og:site_name" content={PELICULA.tituloCompleto} />
    <meta property="og:title" content={PELICULA.tituloCompleto} />
    <meta property="og:description" content={descripcion} />
    <meta property="og:url" content={canonica} />
    <meta property="og:image" content={ogImagen} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="es_ES" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={PELICULA.tituloCompleto} />
    <meta name="twitter:description" content={descripcion} />
    <meta name="twitter:image" content={ogImagen} />

    {/*
      La variable de Archivo es la fuente del título del hero: lo primero que
      pinta la página. Precargarla evita que el navegador la descubra recién
      al parsear el CSS, un paso más tarde de lo necesario.
    */}
    <link
      rel="preload"
      href="/fonts/archivo-variable.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />

    {/*
      Datos estructurados: Google entiende que esto es una película.

      El reemplazo de "<" evita que un valor con "</script>" dentro (por
      ejemplo, si algún día PELICULA.director sale de un CMS y no de un
      archivo local de confianza) cierre la etiqueta antes de tiempo y
      inyecte HTML arbitrario en la página. JSON.stringify no protege de eso
      por sí solo: escapa para contexto JS, no para contexto HTML.
    */}
    <script
      type="application/ld+json"
      set:html={JSON.stringify({
        '@context': 'https://schema.org',
        '@type': 'Movie',
        name: PELICULA.tituloCompleto,
        datePublished: '2026-07-29',
        inLanguage: 'es',
        director: { '@type': 'Person', name: PELICULA.director },
        url: canonica.href,
        image: ogImagen.href,
      }).replace(/</g, '\\u003c')}
    />
  </head>

  <body>
    <a class="saltar" href="#contenido">Saltar al contenido</a>
    <slot />

    <script>
      /**
       * Revelado al scroll, una sola vez para toda la página.
       *
       * IntersectionObserver en lugar del evento 'scroll': el navegador avisa
       * cuando el elemento cruza el umbral, en vez de ejecutar código nuestro
       * en cada pixel scrolleado. Y en cuanto un elemento se revela dejamos de
       * observarlo, así el observer se vacía solo a medida que bajás.
       */
      const objetivos = document.querySelectorAll('.revelar');

      const prefiereQuietud = window.matchMedia(
        '(prefers-reduced-motion: reduce)'
      ).matches;

      if (prefiereQuietud || !('IntersectionObserver' in window)) {
        objetivos.forEach((el) => el.classList.add('visible'));
      } else {
        const observador = new IntersectionObserver(
          (entradas) => {
            for (const entrada of entradas) {
              if (!entrada.isIntersecting) continue;
              entrada.target.classList.add('visible');
              observador.unobserve(entrada.target);
            }
          },
          { threshold: 0.12, rootMargin: '0px 0px -60px 0px' }
        );

        objetivos.forEach((el) => observador.observe(el));
      }
    </script>
  </body>
</html>

--------------------------------------------------------------------------
Cómo se lee un archivo .astro (importante, se repite en todos)
--------------------------------------------------------------------------
Un archivo .astro tiene hasta tres partes:
  1. Entre "---" y "---": JavaScript que corre SOLO durante el build (podés
     importar, calcular variables, leer props). Nunca llega al navegador.
  2. El HTML/JSX-como de la página, usando las variables de arriba entre {}.
  3. Un <style> al final: CSS aislado sólo para este componente.

En Base.astro no hay <style> porque todo el CSS compartido vive en
global.css, importado arriba.

NOTA sobre el favicon.ico/apple-touch-icon.png/og.png que aparecen acá:
todavía no los creamos. Si abrís esto en el navegador ahora, esos <link>
apuntan a archivos que no existen — no rompe nada (el navegador
simplemente no encuentra el ícono), y los vamos a crear en la Parte 16.


================================================================================
 PARTE 7 — PRIMER PUNTO DE CONTROL: UNA PÁGINA MÍNIMA
================================================================================

Antes de escribir los 12 componentes de la página real, probemos que todo
lo de arriba encaja. Creá src/pages/index.astro con esto (temporal, lo
vamos a reemplazar en la Parte 15):

--------------------------------------------------------------------------
ARCHIVO: src/pages/index.astro (VERSIÓN TEMPORAL de esta parte)
--------------------------------------------------------------------------
---
import Base from '../layouts/Base.astro';
---

<Base>
  <main id="contenido" style="padding: 4rem; text-align: center;">
    <h1 class="titulo-seccion">Funciona</h1>
    <p class="parrafo">Si ves esto con la tipografía y el fondo oscuro correctos, Base.astro y global.css están bien conectados.</p>
  </main>
</Base>

Ahora corré:

  npm run dev

Abrí http://localhost:4321 — deberías ver fondo azul oscuro casi negro,
texto "Funciona" en una tipografía condensada y gruesa (o la fuente de
reserva del sistema si todavía no autoalojaste las fuentes: eso es
esperable en este punto, se resuelve en la Parte 17).

Si ves un error en la terminal, es momento de pararse y resolverlo antes
de seguir: seguramente sea una ruta de import mal escrita.


================================================================================
 PARTE 8 — EL HERO Y SUS DOS PRIMERAS ISLAS DE REACT
================================================================================

Acá arranca lo interesante. Vamos a crear, en orden:
  1. Skyline.astro       (el dibujo de la ciudad, sin isla)
  2. ErrorBoundary.jsx    (la red de seguridad que van a usar las 4 islas)
  3. CuentaAtras.jsx      (isla 1: el contador)
  4. BotonCalendario.jsx  (isla 2: el menú de calendario)
  5. Hero.astro           (el componente que junta todo lo anterior)

Creá las carpetas src/components/ y src/components/react/.

--------------------------------------------------------------------------
ARCHIVO: src/components/Skyline.astro
--------------------------------------------------------------------------
---
/**
 * Skyline de Nueva York dibujado con geometría propia.
 *
 * Cero assets externos: ni una imagen que descargar, ni un request extra, y
 * escala perfecto en cualquier pantalla porque son rectángulos, no pixeles.
 * Dos planos a distinta opacidad dan profundidad sin coste.
 */
---

<svg
  class="skyline"
  viewBox="0 0 1200 300"
  preserveAspectRatio="xMidYMax slice"
  aria-hidden="true"
  focusable="false"
>
  <!-- Plano de fondo: más lejos, más lavado por la bruma del amanecer. -->
  <g class="skyline__fondo">
    <rect x="30" y="205" width="120" height="95"></rect>
    <rect x="180" y="175" width="90" height="125"></rect>
    <rect x="300" y="150" width="70" height="150"></rect>
    <rect x="420" y="190" width="110" height="110"></rect>
    <rect x="560" y="160" width="85" height="140"></rect>
    <rect x="690" y="185" width="100" height="115"></rect>
    <rect x="820" y="145" width="75" height="155"></rect>
    <rect x="930" y="195" width="120" height="105"></rect>
    <rect x="1080" y="170" width="95" height="130"></rect>
  </g>

  <!-- Plano delantero: la silueta nítida contra el cielo. -->
  <g class="skyline__frente">
    <rect x="0" y="185" width="85" height="115"></rect>
    <rect x="70" y="145" width="60" height="155"></rect>
    <rect x="120" y="215" width="95" height="85"></rect>
    <rect x="200" y="110" width="70" height="190"></rect>
    <rect x="255" y="165" width="80" height="135"></rect>
    <rect x="325" y="75" width="55" height="225"></rect>
    <rect x="370" y="195" width="105" height="105"></rect>
    <rect x="460" y="140" width="65" height="160"></rect>
    <rect x="515" y="205" width="90" height="95"></rect>
    <rect x="590" y="95" width="75" height="205"></rect>
    <rect x="655" y="170" width="60" height="130"></rect>
    <rect x="705" y="225" width="110" height="75"></rect>
    <rect x="800" y="130" width="70" height="170"></rect>
    <rect x="860" y="190" width="85" height="110"></rect>
    <rect x="935" y="60" width="50" height="240"></rect>
    <rect x="955" y="20" width="6" height="45"></rect>
    <rect x="975" y="155" width="80" height="145"></rect>
    <rect x="1045" y="210" width="95" height="90"></rect>
    <rect x="1125" y="165" width="75" height="135"></rect>
  </g>

  <!-- Una sola ventana encendida. Alguien ya está despierto. -->
  <rect class="skyline__ventana" x="345" y="115" width="9" height="13"></rect>

  <!-- Telaraña tendida entre dos torres. El detalle que cuenta de quién es la ciudad. -->
  <g class="skyline__tela">
    <path d="M335 95 L590 118"></path>
    <path d="M335 95 L462 155 L590 118"></path>
    <path d="M335 95 L470 133 L590 118"></path>
    <path d="M355 103 L448 141"></path>
    <path d="M380 110 L462 155"></path>
  </g>
</svg>

<style>
  .skyline {
    width: 100%;
    height: 100%;
  }

  .skyline__frente {
    fill: var(--noche);
  }

  .skyline__fondo {
    fill: var(--noche);
    opacity: 0.45;
  }

  .skyline__ventana {
    fill: var(--alba);
  }

  .skyline__tela {
    stroke: var(--alba);
    stroke-width: 1;
    fill: none;
    opacity: 0.35;
  }

  /* El parpadeo lentísimo de la ventana: presencia, no distracción. */
  @media (prefers-reduced-motion: no-preference) {
    .skyline__ventana {
      animation: vela 5s ease-in-out infinite;
    }
    @keyframes vela {
      0%,
      100% {
        opacity: 1;
      }
      50% {
        opacity: 0.45;
      }
    }
  }
</style>

--------------------------------------------------------------------------
ARCHIVO: src/components/react/ErrorBoundary.jsx
--------------------------------------------------------------------------
import { Component } from 'react';

/**
 * Red de seguridad por isla.
 *
 * Astro hidrata cada isla por separado, pero React no lo sabe: si
 * `CuentaAtras` tira una excepción de render, por defecto React desmonta todo
 * el árbol donde vive, y en el peor de los casos deja un hueco en blanco
 * donde debería estar el elemento firma del hero. Un error boundary limita el
 * daño a esa isla concreta — el resto de la página, incluidas las otras tres
 * islas, sigue funcionando.
 *
 * Sólo puede escribirse como componente de clase: es la única API de React
 * que expone `getDerivedStateFromError` / `componentDidCatch`.
 */
export default class ErrorBoundary extends Component {
  constructor(props) {
    super(props);
    this.state = { fallo: false };
  }

  static getDerivedStateFromError() {
    return { fallo: true };
  }

  componentDidCatch(error) {
    // Sin backend de analítica: dejamos rastro en la consola para quien
    // depure, sin pretender que hay telemetría que no existe.
    console.error('[landing-spider-man] isla caída:', error);
  }

  render() {
    if (this.state.fallo) {
      return this.props.fallback ?? null;
    }
    return this.props.children;
  }
}

Nota: por qué "componente de clase" y no una función con Hooks. React
todavía no ofrece un Hook equivalente a "atrapar errores de renderizado de
mis hijos" — esa capacidad (getDerivedStateFromError/componentDidCatch)
sólo existe en la API vieja de clases. Es la única excepción en todo el
proyecto a "siempre funciones", y es una limitación de React, no una
elección nuestra.

--------------------------------------------------------------------------
ARCHIVO: src/components/react/CuentaAtras.jsx
--------------------------------------------------------------------------
import { Fragment, useEffect, useState } from 'react';
import ErrorBoundary from './ErrorBoundary.jsx';

/**
 * Cuenta atrás hasta el estreno.
 *
 * Props:
 *   objetivoISO  {string}  Fecha en ISO 8601 **con offset explícito** (ej. +02:00).
 *   mensajeFinal {string}  Texto que reemplaza al contador una vez pasada la fecha.
 */

const dosDigitos = (n) => String(n).padStart(2, '0');

/**
 * Calcula el tiempo que falta leyendo el reloj real en cada tick.
 *
 * No restamos "un segundo" al valor anterior a propósito: cuando la pestaña
 * pasa a segundo plano el navegador estrangula los timers, y un contador que
 * resta acumularía todo ese retraso como error permanente. Recalcular contra
 * Date.now() hace que volver a la pestaña muestre siempre la cifra correcta.
 *
 * Devuelve null cuando la fecha ya pasó.
 */
function calcular(objetivo) {
  const restante = objetivo - Date.now();
  if (restante <= 0) return null;

  const total = Math.floor(restante / 1000);
  return {
    dias: Math.floor(total / 86400),
    horas: Math.floor(total / 3600) % 24,
    minutos: Math.floor(total / 60) % 60,
    segundos: total % 60,
  };
}

function CuentaAtrasBase({ objetivoISO, mensajeFinal = 'Ya en cines' }) {
  const objetivo = new Date(objetivoISO).getTime();

  // Si `pelicula.js` trae una fecha mal formada, `objetivo` es NaN y todas las
  // cuentas de abajo se propagan como NaN: el contador mostraría "NaN:NaN"
  // para siempre en lugar de fallar con un mensaje entendible.
  if (Number.isNaN(objetivo)) {
    return (
      <p className="cuenta cuenta--estreno" role="status">
        Fecha de estreno no disponible
      </p>
    );
  }

  /**
   * Arranca en `undefined`, no en el valor calculado. El HTML se genera en el
   * build, así que si calculáramos aquí el servidor pintaría la cifra de
   * entonces y React encontraría otra al hidratar: error de hidratación y un
   * parpadeo visible. Con `undefined` el marcado del build y el del cliente
   * coinciden, y el número real entra en el primer efecto.
   */
  const [restante, setRestante] = useState(undefined);

  useEffect(() => {
    setRestante(calcular(objetivo));

    const reloj = window.setInterval(() => {
      const siguiente = calcular(objetivo);
      setRestante(siguiente);
      // Se acabó: paramos el intervalo en lugar de latir para siempre.
      if (siguiente === null) window.clearInterval(reloj);
    }, 1000);

    return () => window.clearInterval(reloj);
  }, [objetivo]);

  if (restante === null) {
    return (
      <p className="cuenta cuenta--estreno" role="status">
        {mensajeFinal}
      </p>
    );
  }

  const unidades = [
    { valor: restante ? dosDigitos(restante.dias) : '--', label: 'días' },
    { valor: restante ? dosDigitos(restante.horas) : '--', label: 'horas' },
    { valor: restante ? dosDigitos(restante.minutos) : '--', label: 'min' },
    { valor: restante ? dosDigitos(restante.segundos) : '--', label: 'seg' },
  ];

  return (
    /*
     * aria-hidden a propósito: un lector de pantalla anunciando
     * "42 segundos... 41 segundos..." sin parar vuelve la página inusable.
     * La misma información se da una sola vez en el .sr-only del Hero.
     */
    <div className="cuenta" aria-hidden="true">
      {unidades.map((unidad, i) => (
        <Fragment key={unidad.label}>
          {i > 0 && <div className="cuenta__sep" />}
          <div className="cuenta__unidad">
            <span className="cuenta__numero">{unidad.valor}</span>
            <span className="cuenta__label">{unidad.label}</span>
          </div>
        </Fragment>
      ))}
    </div>
  );
}

/**
 * El límite de error vive acá, no en el .astro que la usa: así Astro sigue
 * importando `CuentaAtras` igual que siempre y no hace falta anidar
 * componentes de dos frameworks en el marcado.
 */
export default function CuentaAtras(props) {
  const fecha = new Date(props.objetivoISO);
  const fechaLegible = Number.isNaN(fecha.getTime())
    ? 'Fecha de estreno no disponible'
    : `Estreno: ${fecha.toLocaleDateString('es-ES', { day: 'numeric', month: 'long', year: 'numeric' })}`;

  return (
    <ErrorBoundary
      fallback={
        <p className="cuenta cuenta--estreno" role="status">
          {fechaLegible}
        </p>
      }
    >
      <CuentaAtrasBase {...props} />
    </ErrorBoundary>
  );
}

--------------------------------------------------------------------------
ARCHIVO: src/components/react/BotonCalendario.jsx
--------------------------------------------------------------------------
import { useEffect, useId, useRef, useState } from 'react';
import ErrorBoundary from './ErrorBoundary.jsx';

/**
 * Botón "Añadir al calendario" con menú de destinos.
 *
 * Props:
 *   titulo      {string} Nombre del evento.
 *   inicio      {string} Día de inicio en formato ICS: YYYYMMDD.
 *   fin         {string} Día de fin, exclusivo (el día siguiente al del evento).
 *   descripcion {string} Texto largo del evento.
 *   variante    {'solido'|'borde'}
 *   alineacion  {'izquierda'|'derecha'} Ancla el menú al otro lado si el botón
 *                                       está pegado al borde de la pantalla.
 */

/**
 * Escapa los caracteres que el formato iCalendar (RFC 5545) trata como
 * sintaxis. Sin esto, un título con una coma parte el campo en dos y algunos
 * clientes de calendario descartan el evento entero sin avisar.
 */
const escaparICS = (texto) =>
  texto.replace(/\\/g, '\\\\').replace(/;/g, '\\;').replace(/,/g, '\\,').replace(/\n/g, '\\n');

/** 20260729 → 2026-07-29, que es lo que espera el deeplink de Outlook. */
const conGuiones = (fecha) =>
  `${fecha.slice(0, 4)}-${fecha.slice(4, 6)}-${fecha.slice(6, 8)}`;

function BotonCalendarioBase({
  titulo,
  inicio,
  fin,
  descripcion = '',
  variante = 'borde',
  alineacion = 'izquierda',
}) {
  const [abierto, setAbierto] = useState(false);
  const [aviso, setAviso] = useState('');
  const contenedor = useRef(null);
  const idMenu = useId();

  // Cerrar al hacer click fuera o al pulsar Escape: lo que espera cualquiera
  // que haya usado un menú antes. Los listeners sólo existen mientras el menú
  // está abierto, así que la página en reposo no escucha nada.
  useEffect(() => {
    if (!abierto) return;

    const alClickFuera = (e) => {
      if (!contenedor.current?.contains(e.target)) setAbierto(false);
    };
    const alTeclado = (e) => {
      if (e.key === 'Escape') setAbierto(false);
    };

    document.addEventListener('mousedown', alClickFuera);
    document.addEventListener('keydown', alTeclado);
    return () => {
      document.removeEventListener('mousedown', alClickFuera);
      document.removeEventListener('keydown', alTeclado);
    };
  }, [abierto]);

  /**
   * Genera el .ics en el propio navegador con un Blob: sin backend, sin
   * librería de 40 KB y sin mandar a nadie a Google si no quiere.
   *
   * Envuelto en try/catch porque `Blob`/`URL.createObjectURL` pueden fallar
   * en navegadores muy viejos o en modos de privacidad estrictos: si pasa,
   * el menú se queda abierto con los enlaces a Google/Outlook como plan B en
   * lugar de que el click no haga nada y parezca que la página está rota.
   */
  const descargarICS = () => {
    try {
      const ics = [
        'BEGIN:VCALENDAR',
        'VERSION:2.0',
        'PRODID:-//landing-spider-man//ES',
        'CALSCALE:GREGORIAN',
        'BEGIN:VEVENT',
        `UID:estreno-${inicio}@landing-spider-man`,
        `DTSTAMP:${inicio}T000000Z`,
        `DTSTART;VALUE=DATE:${inicio}`,
        `DTEND;VALUE=DATE:${fin}`,
        `SUMMARY:${escaparICS(titulo)}`,
        `DESCRIPTION:${escaparICS(descripcion)}`,
        'BEGIN:VALARM',
        'TRIGGER:-P1D',
        'ACTION:DISPLAY',
        'DESCRIPTION:Mañana se estrena',
        'END:VALARM',
        'END:VEVENT',
        'END:VCALENDAR',
      ].join('\r\n'); // El RFC exige CRLF. Con \n solo, Outlook rechaza el archivo.

      const url = URL.createObjectURL(
        new Blob([ics], { type: 'text/calendar;charset=utf-8' })
      );
      const enlace = document.createElement('a');
      enlace.href = url;
      enlace.download = 'spider-man-brand-new-day.ics';
      enlace.click();
      URL.revokeObjectURL(url); // Liberamos el objeto: si no, vive hasta recargar.

      setAbierto(false);
      setAviso('Archivo descargado');
      window.setTimeout(() => setAviso(''), 3000);
    } catch {
      setAviso('No se pudo descargar. Probá con Google o Outlook.');
      window.setTimeout(() => setAviso(''), 4000);
    }
  };

  const urlGoogle = new URL('https://calendar.google.com/calendar/render');
  urlGoogle.searchParams.set('action', 'TEMPLATE');
  urlGoogle.searchParams.set('text', titulo);
  urlGoogle.searchParams.set('dates', `${inicio}/${fin}`);
  urlGoogle.searchParams.set('details', descripcion);

  const urlOutlook = new URL('https://outlook.live.com/calendar/0/deeplink/compose');
  urlOutlook.searchParams.set('path', '/calendar/action/compose');
  urlOutlook.searchParams.set('subject', titulo);
  urlOutlook.searchParams.set('allday', 'true');
  urlOutlook.searchParams.set('startdt', conGuiones(inicio));
  urlOutlook.searchParams.set('enddt', conGuiones(inicio));
  urlOutlook.searchParams.set('body', descripcion);

  return (
    <div
      className={`cal ${alineacion === 'derecha' ? 'cal--derecha' : ''}`}
      data-abierto={abierto}
      ref={contenedor}
    >
      <button
        type="button"
        className={`boton ${variante === 'solido' ? 'boton--solido' : ''}`}
        aria-expanded={abierto}
        aria-haspopup="menu"
        aria-controls={idMenu}
        onClick={() => setAbierto((v) => !v)}
      >
        Añadir al calendario
        <svg className="cal__flecha" viewBox="0 0 12 8" aria-hidden="true">
          <path d="M1 1.5 6 6.5l5-5" fill="none" stroke="currentColor" strokeWidth="1.8" />
        </svg>
      </button>

      {abierto && (
        <ul className="cal__menu" id={idMenu} role="menu">
          <li role="none">
            <button type="button" role="menuitem" className="cal__opcion" onClick={descargarICS}>
              <IconoDescarga />
              Descargar .ics (Apple, Outlook…)
            </button>
          </li>
          <li role="none">
            <a
              role="menuitem"
              className="cal__opcion"
              href={urlGoogle.toString()}
              target="_blank"
              rel="noopener noreferrer"
              onClick={() => setAbierto(false)}
            >
              <IconoEnlace />
              Google Calendar
            </a>
          </li>
          <li role="none">
            <a
              role="menuitem"
              className="cal__opcion"
              href={urlOutlook.toString()}
              target="_blank"
              rel="noopener noreferrer"
              onClick={() => setAbierto(false)}
            >
              <IconoEnlace />
              Outlook.com
            </a>
          </li>
        </ul>
      )}

      {/* role=status: el lector de pantalla anuncia la confirmación sin robar el foco. */}
      {aviso && (
        <p className="cal__aviso" role="status">
          {aviso}
        </p>
      )}
    </div>
  );
}

export default function BotonCalendario(props) {
  return (
    <ErrorBoundary
      fallback={
        <p className="parrafo cal__fallback" role="status">
          No se pudo cargar el botón de calendario.
        </p>
      }
    >
      <BotonCalendarioBase {...props} />
    </ErrorBoundary>
  );
}

const IconoDescarga = () => (
  <svg className="cal__icono" viewBox="0 0 20 20" aria-hidden="true">
    <path d="M10 3v10m0 0 4-4m-4 4-4-4M3 16h14" strokeLinecap="round" strokeLinejoin="round" />
  </svg>
);

const IconoEnlace = () => (
  <svg className="cal__icono" viewBox="0 0 20 20" aria-hidden="true">
    <path
      d="M11 3h6v6M17 3l-8 8M15 12v4a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1h4"
      strokeLinecap="round"
      strokeLinejoin="round"
    />
  </svg>
);

--------------------------------------------------------------------------
ARCHIVO: src/components/Hero.astro
--------------------------------------------------------------------------
---
import Skyline from './Skyline.astro';
import CuentaAtras from './react/CuentaAtras.jsx';
import BotonCalendario from './react/BotonCalendario.jsx';
import { PELICULA } from '../data/pelicula.js';
---

<section class="hero" id="inicio">
  <div class="hero__cielo" aria-hidden="true"></div>

  <div class="hero__skyline">
    <Skyline />
  </div>

  <div class="hero__contenido">
    <p class="hero__eyebrow">Solo en cines &middot; {PELICULA.estrenoLargo}</p>

    <h1 class="hero__titulo">
      <span class="hero__titulo-a">{PELICULA.titulo}</span>
      <span class="hero__titulo-b">{PELICULA.subtitulo}</span>
    </h1>

    {/*
      client:load — es lo primero que ve el visitante y tiene que estar contando
      desde el primer instante. Cualquier estrategia diferida dejaría "--" a la
      vista durante el arranque, justo en el elemento firma de la página.
    */}
    <CuentaAtras client:load objetivoISO={PELICULA.estrenoISO} />

    {/* La información que el contador esconde al lector de pantalla, dicha una vez. */}
    <p class="sr-only">
      El estreno de {PELICULA.tituloCompleto} es el {PELICULA.estrenoLargo}.
    </p>

    <div class="acciones hero__acciones">
      <a href="#trailer" class="boton boton--solido">Ver el tráiler</a>
      {/*
        client:visible — el menú sólo importa cuando alguien puede pulsarlo.
        Su JS no compite con el del contador durante la carga inicial.
      */}
      <BotonCalendario
        client:visible
        titulo={`${PELICULA.tituloCompleto} — Estreno`}
        inicio={PELICULA.estrenoICS.inicio}
        fin={PELICULA.estrenoICS.fin}
        descripcion="Estreno en cines."
      />
    </div>
  </div>

  <a class="hero__bajar" href="#sinopsis" aria-label="Ir a la sinopsis">
    <span aria-hidden="true"></span>
  </a>
</section>

<style>
  .hero {
    /* svh y no vh: descuenta la barra del navegador móvil, que en vh provoca
       que el contenido quede cortado justo en los teléfonos. */
    min-height: 100svh;
    display: grid;
    overflow: hidden;
  }

  /* Los hijos comparten la misma celda del grid: se apilan sin position
     absolute y sin una sola pelea de z-index. */
  .hero > * {
    grid-area: 1 / 1;
  }

  .hero__cielo {
    background: linear-gradient(
      to top,
      var(--alba) 0%,
      var(--horizonte) 14%,
      var(--cielo) 40%,
      var(--medianoche) 72%,
      var(--noche) 100%
    );
  }

  .hero__skyline {
    align-self: end;
    width: 100%;
    height: min(42vh, 340px);
  }

  .hero__contenido {
    align-self: center;
    justify-self: center;
    text-align: center;
    padding: var(--espacio);
    padding-bottom: min(26vh, 220px);
    max-width: 62rem;
  }

  .hero__eyebrow {
    font-family: var(--mono);
    font-size: clamp(0.62rem, 1.5vw, 0.78rem);
    letter-spacing: 0.28em;
    text-transform: uppercase;
    opacity: 0.75;
    margin-bottom: clamp(0.75rem, 2vw, 1.5rem);
  }

  /* Todo el tratamiento del título vive en el eje de ancho de Archivo:
     condensado + negro arriba, expandido + fino abajo. Una sola familia. */
  .hero__titulo {
    display: flex;
    flex-direction: column;
    line-height: 0.82;
  }

  .hero__titulo-a {
    font-family: var(--display);
    font-variation-settings: 'wdth' 62.5, 'wght' 900;
    font-size: clamp(3.4rem, 14.5vw, 10.5rem);
    text-transform: uppercase;
    letter-spacing: -0.012em;
  }

  .hero__titulo-b {
    font-family: var(--display);
    font-variation-settings: 'wdth' 110, 'wght' 400;
    font-size: clamp(0.85rem, 3.3vw, 2.3rem);
    text-transform: uppercase;
    letter-spacing: 0.42em;
    /* Compensa el tracking sobrante a la derecha del último carácter. */
    text-indent: 0.42em;
    color: var(--alba);
    margin-top: 0.7em;
  }

  .hero__acciones {
    justify-content: center;
    margin-top: clamp(1.75rem, 4vw, 2.75rem);
  }

  .hero__bajar {
    align-self: end;
    justify-self: center;
    margin-bottom: 1.75rem;
    width: 1px;
    height: 3.5rem;
    background: var(--luz);
    opacity: 0.35;
    overflow: hidden;
  }

  .hero__bajar span {
    display: block;
    width: 100%;
    height: 40%;
    background: var(--alba);
  }

  @media (prefers-reduced-motion: no-preference) {
    .hero__bajar span {
      animation: gotear 2.4s ease-in-out infinite;
    }
    @keyframes gotear {
      0% {
        transform: translateY(-100%);
      }
      60%,
      100% {
        transform: translateY(250%);
      }
    }
  }

  @media (max-width: 40rem) {
    .hero__bajar {
      display: none;
    }
  }
</style>

--------------------------------------------------------------------------
Las dos instrucciones "client:*" — la decisión más importante del archivo
--------------------------------------------------------------------------
  client:load     → activá el JavaScript de esta isla apenas la página
                     carga. Se usa en CuentaAtras porque tiene que estar
                     contando desde el primer segundo.
  client:visible  → activá el JavaScript recién cuando esta parte de la
                     pantalla entra en el viewport. Se usa en
                     BotonCalendario (y, más adelante, en Trailer y
                     Entradas) porque pueden esperar.

Sin ninguna de las dos, Astro renderiza el componente como HTML puro en el
build pero NUNCA le agrega interactividad — se vería pero los botones no
harían nada. Ese es justamente el motivo de que estas palabras existan:
vos decidís, componente por componente, cuánto JavaScript se manda y
cuándo.


================================================================================
 PARTE 9 — ATMÓSFERA, NAVEGACIÓN Y SINOPSIS
================================================================================

--------------------------------------------------------------------------
ARCHIVO: src/components/Atmosfera.astro
--------------------------------------------------------------------------
---
/**
 * Capa fija con el resplandor del amanecer.
 *
 * Su opacidad crece con el scroll: bajar por la página es que pase el tiempo y
 * amanezca sobre la ciudad. Está atada a `animation-timeline: scroll()`, así
 * que la anima el compositor sin una línea de JavaScript y sin un solo evento
 * de scroll. Donde el navegador no lo soporta queda el degradado fijo, que ya
 * se ve bien por sí solo: mejora progresiva, no dependencia.
 */
---

<div class="atmosfera" aria-hidden="true"></div>

<style>
  .atmosfera {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    opacity: 0.25;
    background:
      radial-gradient(120% 65% at 50% 118%, var(--alba) 0%, transparent 62%),
      radial-gradient(140% 80% at 50% 128%, var(--horizonte) 0%, transparent 70%);
  }

  @supports (animation-timeline: scroll()) {
    @media (prefers-reduced-motion: no-preference) {
      .atmosfera {
        animation: amanecer linear both;
        animation-timeline: scroll(root block);
      }

      @keyframes amanecer {
        from {
          opacity: 0;
        }
        to {
          opacity: 0.85;
        }
      }
    }
  }
</style>

--------------------------------------------------------------------------
ARCHIVO: src/components/Navegacion.astro
--------------------------------------------------------------------------
---
import { NAVEGACION, PELICULA } from '../data/pelicula.js';
---

<header class="nav" data-nav>
  <div class="nav__interior">
    <a class="nav__marca" href="#inicio">
      <span class="nav__marca-a">Spider-Man</span>
      <span class="nav__marca-b">Brand New Day</span>
    </a>

    <nav class="nav__enlaces" aria-label="Secciones de la página">
      {
        NAVEGACION.map((enlace) => (
          <a class="nav__enlace" href={`#${enlace.id}`} data-enlace={enlace.id}>
            {enlace.texto}
          </a>
        ))
      }
    </nav>

    <p class="nav__fecha" aria-hidden="true">{PELICULA.estrenoDia} JUL</p>
  </div>
</header>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    height: var(--altura-nav);
    display: flex;
    align-items: center;
    /* Arranca invisible sobre el hero y aparece cuando el scroll la justifica. */
    background: transparent;
    border-bottom: 1px solid transparent;
    transition:
      background var(--transicion),
      border-color var(--transicion),
      backdrop-filter var(--transicion);
  }

  .nav[data-fijada='true'] {
    background: rgb(11 16 38 / 0.82);
    backdrop-filter: blur(14px) saturate(140%);
    border-bottom-color: var(--borde);
  }

  .nav__interior {
    width: var(--ancho-nav);
    margin-inline: auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
  }

  .nav__marca {
    display: flex;
    flex-direction: column;
    line-height: 1;
    text-decoration: none;
    color: var(--luz);
  }

  .nav__marca-a {
    font-family: var(--display);
    font-variation-settings: 'wdth' 70, 'wght' 800;
    font-size: 1.05rem;
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .nav__marca-b {
    font-family: var(--mono);
    font-size: 0.5rem;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    color: var(--alba);
    margin-top: 0.35em;
  }

  .nav__enlaces {
    display: flex;
    gap: clamp(1rem, 2.4vw, 2rem);
  }

  .nav__enlace {
    position: relative;
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-decoration: none;
    color: var(--luz);
    opacity: 0.7;
    padding-block: 0.4rem;
    transition: opacity var(--transicion);
  }

  .nav__enlace::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    height: 1px;
    width: 100%;
    background: var(--alba);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--transicion);
  }

  .nav__enlace:hover,
  .nav__enlace[aria-current='true'] {
    opacity: 1;
  }
  .nav__enlace:hover::after,
  .nav__enlace[aria-current='true']::after {
    transform: scaleX(1);
  }

  .nav__fecha {
    font-family: var(--mono);
    font-size: 0.68rem;
    letter-spacing: 0.2em;
    color: var(--luz);
    opacity: 0.55;
  }

  /* En pantallas estrechas el menú de anclas estorba más de lo que ayuda:
     se va, y quedan la marca y la fecha, que es lo que hay que recordar. */
  @media (max-width: 52rem) {
    .nav__enlaces {
      display: none;
    }
  }
</style>

<script>
  /**
   * Dos comportamientos, un solo observer cada uno. Cero listeners de 'scroll'.
   */
  const barra = document.querySelector('[data-nav]');

  if (barra) {
    // 1. Fondo sólido en cuanto el hero deja de tocar el borde superior.
    //    Un centinela de 1px al principio del documento: mientras se vea,
    //    estamos arriba del todo.
    const centinela = document.createElement('div');
    centinela.setAttribute('aria-hidden', 'true');
    centinela.style.cssText = 'position:absolute;top:0;height:1px;width:1px;';
    document.body.prepend(centinela);

    new IntersectionObserver(
      ([entrada]) => {
        barra.dataset.fijada = String(!entrada.isIntersecting);
      },
      { threshold: 0 }
    ).observe(centinela);

    // 2. Scroll-spy: marca el enlace de la sección visible.
    const enlaces = new Map();
    document
      .querySelectorAll('[data-enlace]')
      .forEach((a) => enlaces.set(a.dataset.enlace, a));

    const secciones = [...enlaces.keys()]
      .map((id) => document.getElementById(id))
      .filter((el) => el !== null);

    const espia = new IntersectionObserver(
      (entradas) => {
        for (const entrada of entradas) {
          const enlace = enlaces.get(entrada.target.id);
          if (!enlace) continue;
          if (entrada.isIntersecting) enlace.setAttribute('aria-current', 'true');
          else enlace.removeAttribute('aria-current');
        }
      },
      // La banda activa es el tercio central de la pantalla: así sólo hay una
      // sección "actual" a la vez, sin parpadeos en las transiciones.
      { rootMargin: '-35% 0px -55% 0px' }
    );

    secciones.forEach((s) => espia.observe(s));
  }
</script>

Nota: este <script> de acá NO es una isla de React. Es JavaScript nativo
del navegador, que Astro empaqueta y sirve como un archivo aparte
automáticamente. No hace falta client:load porque no es un componente de
framework — es simplemente HTML con su propio script, como en los años 90,
pero empaquetado moderno.

--------------------------------------------------------------------------
ARCHIVO: src/components/Sinopsis.astro
--------------------------------------------------------------------------
---
const CIFRAS = [
  { valor: '29.07', etiqueta: 'Estreno mundial' },
  { valor: 'IMAX', etiqueta: 'Rodada para pantalla grande' },
  { valor: '2026', etiqueta: 'Fase seis' },
];
---

<section class="bloque sinopsis" id="sinopsis">
  <div class="sinopsis__texto">
    <p class="eyebrow revelar">La noche termina</p>
    <h2 class="titulo-seccion revelar" data-retraso="1">
      Toda ciudad<br />amanece
    </h2>

    {/* Sustituir por la sinopsis oficial cuando llegue el press kit. */}
    <p class="parrafo revelar" data-retraso="2">
      Nueva York no duerme: espera. Peter Parker tampoco. Después de perderlo
      todo — el nombre, la gente, el sitio al que volver — arranca de cero en
      una ciudad que ya no sabe quién es.
    </p>
    <p class="parrafo revelar" data-retraso="3">
      Y cuando el sol pega en el primer rascacielos, alguien ya está en el aire.
    </p>
  </div>

  <ul class="sinopsis__cifras">
    {
      CIFRAS.map((cifra, i) => (
        <li class="cifra revelar" data-retraso={String(i + 1)}>
          <p class="cifra__valor">{cifra.valor}</p>
          <p class="cifra__etiqueta">{cifra.etiqueta}</p>
        </li>
      ))
    }
  </ul>
</section>

<style>
  .sinopsis {
    display: grid;
    gap: clamp(2.5rem, 6vw, 5rem);
    grid-template-columns: 1fr;
    align-items: start;
  }

  @media (min-width: 60rem) {
    .sinopsis {
      grid-template-columns: 1.35fr 1fr;
    }
  }

  .sinopsis__texto {
    display: flex;
    flex-direction: column;
    gap: 1.35rem;
  }

  .sinopsis__cifras {
    list-style: none;
    display: grid;
    gap: 1px;
    background: var(--borde);
    border: 1px solid var(--borde);
  }

  .cifra {
    padding: clamp(1.25rem, 3vw, 1.9rem);
    background: var(--noche);
  }

  .cifra__valor {
    font-family: var(--display);
    font-variation-settings: 'wdth' 75, 'wght' 800;
    font-size: clamp(1.6rem, 4vw, 2.4rem);
    line-height: 1;
    text-transform: uppercase;
    color: var(--alba);
  }

  .cifra__etiqueta {
    font-size: 0.8rem;
    letter-spacing: 0.04em;
    color: var(--luz-tenue);
    margin-top: 0.5rem;
  }
</style>

Fijate la clase "revelar" con "data-retraso": eso lo activa el script de
Base.astro que ya escribimos en la Parte 6. Cada elemento con "revelar"
arranca invisible y sube con un fade cuando entra en pantalla; el
"data-retraso" (1, 2, 3...) hace que no todos aparezcan al mismo tiempo,
sino en cascada.


================================================================================
 PARTE 10 — EL TRÁILER (PATRÓN FACADE)
================================================================================

--------------------------------------------------------------------------
ARCHIVO: src/components/react/Trailer.jsx
--------------------------------------------------------------------------
import { useState } from 'react';
import ErrorBoundary from './ErrorBoundary.jsx';

/**
 * Reproductor con patrón *facade*.
 *
 * Un iframe de YouTube arrastra varios cientos de KB de JS y abre conexiones a
 * tres dominios en cuanto aparece en el DOM — lo dé play alguien o no. Acá la
 * página no le pide absolutamente nada a YouTube hasta el click: mostramos una
 * portada propia (un degradado, cero bytes de red) y recién entonces creamos
 * el iframe con autoplay. En una landing que espera un pico de tráfico, es la
 * diferencia entre un LCP sano y uno arrastrado por un vídeo que nadie miró.
 *
 * Props:
 *   videoId {string}  ID de YouTube. Vacío = el tráiler todavía no salió.
 *   titulo  {string}  Nombre de la película, para los textos accesibles.
 */
function TrailerBase({ videoId = '', titulo }) {
  const [reproduciendo, setReproduciendo] = useState(false);
  const disponible = videoId.trim().length > 0;

  if (reproduciendo) {
    return (
      <div className="trailer">
        <iframe
          src={`https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&rel=0&modestbranding=1`}
          title={`Tráiler de ${titulo}`}
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowFullScreen
        />
      </div>
    );
  }

  return (
    <div className="trailer">
      <button
        type="button"
        className="trailer__boton"
        disabled={!disponible}
        onClick={() => setReproduciendo(true)}
        aria-label={
          disponible
            ? `Reproducir el tráiler de ${titulo}`
            : 'El tráiler todavía no está disponible'
        }
      >
        <span
          className={`trailer__icono ${disponible ? 'trailer__icono--play' : ''}`}
          aria-hidden="true"
        >
          {disponible ? (
            <svg viewBox="0 0 24 24">
              <path d="M8 5v14l11-7z" />
            </svg>
          ) : (
            <svg viewBox="0 0 24 24">
              <circle cx="12" cy="12" r="9" fill="none" stroke="currentColor" strokeWidth="2" />
              <path
                d="M12 7v5l3 2"
                fill="none"
                stroke="currentColor"
                strokeWidth="2"
                strokeLinecap="round"
              />
            </svg>
          )}
        </span>

        <span>{disponible ? 'Reproducir tráiler' : 'Tráiler próximamente'}</span>

        {!disponible && (
          <span className="trailer__nota">
            El primer avance oficial aterriza en las próximas semanas.
          </span>
        )}
      </button>
    </div>
  );
}

export default function Trailer(props) {
  return (
    <ErrorBoundary
      fallback={
        <div className="trailer trailer--fallback">
          <p className="trailer__nota" role="status">
            El tráiler no se pudo cargar. Volvé a intentarlo en unos minutos.
          </p>
        </div>
      }
    >
      <TrailerBase {...props} />
    </ErrorBoundary>
  );
}

--------------------------------------------------------------------------
ARCHIVO: src/components/SeccionTrailer.astro
--------------------------------------------------------------------------
---
import Trailer from './react/Trailer.jsx';
import { PELICULA } from '../data/pelicula.js';
---

<section class="bloque" id="trailer">
  <div class="bloque__cabecera">
    <p class="eyebrow revelar">Tráiler oficial</p>
    <h2 class="titulo-seccion revelar" data-retraso="1">Miralo</h2>
  </div>

  <div class="revelar" data-retraso="2">
    {/*
      client:visible — no hay motivo para hidratar un reproductor que está
      tres pantallas más abajo. Su JS se descarga cuando la sección entra en
      pantalla, no antes.
    */}
    <Trailer
      client:visible
      videoId={PELICULA.trailerId}
      titulo={PELICULA.tituloCompleto}
    />
  </div>
</section>

Por qué youtube-nocookie.com y no youtube.com: es un dominio alternativo
que el propio YouTube ofrece, pensado para no guardar cookies de
seguimiento hasta que el usuario le da play de verdad — una capa extra de
privacidad, gratis.


================================================================================
 PARTE 11 — REPARTO
================================================================================

--------------------------------------------------------------------------
ARCHIVO: src/components/Reparto.astro
--------------------------------------------------------------------------
---
import { PELICULA, REPARTO } from '../data/pelicula.js';

/** Iniciales como retrato: sin fotos con derechos, sin peso de red. */
const iniciales = (nombre) =>
  nombre
    .split(' ')
    .slice(0, 2)
    .map((parte) => parte[0])
    .join('');

const FICHA = [
  { clave: 'Dirección', valor: PELICULA.director },
  { clave: 'Producción', valor: PELICULA.estudios },
  { clave: 'Estreno', valor: PELICULA.estrenoLargo },
  { clave: 'Idiomas', valor: PELICULA.idiomas },
  { clave: 'Calificación', valor: PELICULA.clasificacion },
];
---

<section class="bloque" id="reparto">
  <div class="bloque__cabecera">
    <p class="eyebrow revelar">Quién está detrás</p>
    <h2 class="titulo-seccion revelar" data-retraso="1">Reparto</h2>
  </div>

  <ul class="reparto">
    {
      REPARTO.map((persona, i) => (
        <li class="actor revelar" data-retraso={String((i % 4) + 1)}>
          <span class="actor__retrato" aria-hidden="true">
            {iniciales(persona.actor)}
          </span>
          <p class="actor__nombre">{persona.actor}</p>
          <p class="actor__personaje">{persona.personaje}</p>
        </li>
      ))
    }
  </ul>

  <dl class="ficha">
    {
      FICHA.map((fila) => (
        <div class="ficha__fila revelar">
          <dt class="ficha__clave">{fila.clave}</dt>
          <dd class="ficha__valor">{fila.valor}</dd>
        </div>
      ))
    }
  </dl>

  <p class="reparto__aviso">
    Reparto según anuncios públicos de prensa. Sujeto a cambios hasta el estreno.
  </p>
</section>

<style>
  /* Columnas fijas y no auto-fill a propósito: el reparto tiene 8 fichas, y
     con 1, 2 o 4 columnas las filas siempre quedan completas. Con auto-fill el
     número de columnas depende del ancho y las celdas sobrantes dejaban ver el
     color de las separaciones como huecos más claros. */
  .reparto {
    list-style: none;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1px;
    background: var(--borde);
    border: 1px solid var(--borde);
  }

  @media (min-width: 34rem) {
    .reparto {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 62rem) {
    .reparto {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .actor {
    background: var(--noche);
    padding: clamp(1.25rem, 3vw, 1.75rem);
    transition: background var(--transicion);
  }
  .actor:hover {
    background: var(--medianoche);
  }

  .actor__retrato {
    display: grid;
    place-items: center;
    width: 3rem;
    height: 3rem;
    margin-bottom: 1rem;
    border: 1px solid var(--borde-fuerte);
    border-radius: 50%;
    font-family: var(--mono);
    font-size: 0.85rem;
    letter-spacing: 0.05em;
    color: var(--alba);
  }

  .actor__nombre {
    font-family: var(--display);
    font-variation-settings: 'wdth' 88, 'wght' 700;
    font-size: 1.05rem;
    line-height: 1.2;
  }

  .actor__personaje {
    font-size: 0.82rem;
    color: var(--luz-tenue);
    margin-top: 0.35rem;
  }

  .ficha {
    margin-top: clamp(2.5rem, 5vw, 3.5rem);
    border-top: 1px solid var(--borde);
  }

  .ficha__fila {
    display: grid;
    grid-template-columns: minmax(8rem, 14rem) 1fr;
    gap: 1rem;
    padding-block: 1rem;
    border-bottom: 1px solid var(--borde);
  }

  @media (max-width: 40rem) {
    .ficha__fila {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }
  }

  .ficha__clave {
    font-family: var(--mono);
    font-size: 0.68rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--luz-tenue);
    padding-top: 0.25rem;
  }

  .ficha__valor {
    font-size: 1rem;
  }

  .reparto__aviso {
    margin-top: 1.5rem;
    font-size: 0.78rem;
    color: var(--luz-tenue);
  }
</style>

Ojo con "grid-template-columns: 1fr" fijo y no "auto-fill": con 8 fichas y
columnas automáticas, según el ancho de pantalla podés terminar con una
fila incompleta (por ejemplo 3 columnas y 8 elementos = una celda vacía al
final, que se ve como un hueco de otro color). Con columnas fijas en 1, 2 o
4, ocho fichas siempre completan la última fila.


================================================================================
 PARTE 12 — SESIONES (LA ISLA DE FILTRADO)
================================================================================

--------------------------------------------------------------------------
ARCHIVO: src/components/react/Entradas.jsx
--------------------------------------------------------------------------
import { useMemo, useState } from 'react';
import ErrorBoundary from './ErrorBoundary.jsx';

const FORMATOS = ['Todos', 'IMAX', '4DX', 'V.O.S.E.', 'Digital'];

/**
 * Selector de sesiones por ciudad y formato.
 *
 * Todo el filtrado ocurre en memoria sobre datos que ya viajaron con el HTML:
 * cambiar de ciudad no dispara una petición ni un repintado de la página. Es
 * el tipo de interacción que justifica traer React a una landing por lo demás
 * estática — y la isla pesa unos pocos KB, no el sitio entero.
 *
 * Los datos son de demostración. Cuando exista el endpoint real basta con
 * reemplazar la prop `cines` por la respuesta de la API: la vista no cambia.
 *
 * Props:
 *   ciudades {string[]}  Nombres de ciudad, en orden.
 *   cines    {object}    { [ciudad]: [{ cine, sala, horas, formato }] }
 */
function EntradasBase({ ciudades = [], cines = {} }) {
  const [ciudad, setCiudad] = useState(ciudades[0] ?? '');
  const [formato, setFormato] = useState('Todos');

  const sesiones = useMemo(() => {
    const todas = cines[ciudad] ?? [];
    return formato === 'Todos' ? todas : todas.filter((s) => s.formato === formato);
  }, [ciudad, formato, cines]);

  // Sin datos de ciudades no hay nada que filtrar: en vez de una lista de
  // pestañas vacía y un mensaje confuso ("sesiones para "), avisamos directo.
  if (ciudades.length === 0) {
    return (
      <p className="parrafo" role="status">
        Todavía no hay sesiones publicadas. Volvé más cerca del estreno.
      </p>
    );
  }

  return (
    <div>
      {/* role=tablist para que el lector de pantalla lea "pestaña 2 de 5". */}
      <div className="sesiones__ciudades" role="tablist" aria-label="Elegí tu ciudad">
        {ciudades.map((c) => (
          <button
            key={c}
            type="button"
            role="tab"
            aria-selected={c === ciudad}
            className="sesiones__ciudad"
            onClick={() => setCiudad(c)}
          >
            {c}
          </button>
        ))}
      </div>

      <div className="sesiones__ciudades" role="group" aria-label="Filtrar por formato">
        {FORMATOS.map((f) => (
          <button
            key={f}
            type="button"
            aria-pressed={f === formato}
            className="sesiones__ciudad"
            data-activo={f === formato}
            onClick={() => setFormato(f)}
          >
            {f}
          </button>
        ))}
      </div>

      {sesiones.length === 0 ? (
        <p className="parrafo" role="status">
          Todavía no hay sesiones en {formato} para {ciudad}. Probá con otro formato
          o volvé cuando abra la venta anticipada.
        </p>
      ) : (
        <ul className="sesiones__lista">
          {sesiones.map((s) => (
            <li className="sesion" key={`${s.cine}-${s.sala}`}>
              <div>
                <p className="sesion__cine">{s.cine}</p>
                <p className="sesion__meta">
                  <span className="sesion__formato">{s.formato}</span>
                  <span>{s.sala}</span>
                  <span>{ciudad}</span>
                </p>
              </div>

              <div className="sesion__horas">
                {s.horas.map((hora) => (
                  <button
                    key={hora}
                    type="button"
                    className="sesion__hora"
                    aria-label={`Sesión de las ${hora} en ${s.cine}, ${s.formato}`}
                  >
                    {hora}
                  </button>
                ))}
              </div>
            </li>
          ))}
        </ul>
      )}

      <p className="sesiones__aviso">
        Horarios de demostración. La venta anticipada todavía no está abierta.
      </p>
    </div>
  );
}

export default function Entradas(props) {
  return (
    <ErrorBoundary
      fallback={
        <p className="parrafo" role="status">
          No se pudieron cargar las sesiones. Recargá la página para
          reintentarlo.
        </p>
      }
    >
      <EntradasBase {...props} />
    </ErrorBoundary>
  );
}

--------------------------------------------------------------------------
ARCHIVO: src/components/Sesiones.astro
--------------------------------------------------------------------------
---
import Entradas from './react/Entradas.jsx';
import { CINES, CIUDADES } from '../data/pelicula.js';
---

<section class="bloque" id="sesiones">
  <div class="bloque__cabecera">
    <p class="eyebrow revelar">Dónde verla</p>
    <h2 class="titulo-seccion revelar" data-retraso="1">Sesiones</h2>
    <p class="parrafo revelar" data-retraso="2">
      Elegí tu ciudad y el formato. El filtrado ocurre en el navegador sobre
      datos que ya viajaron con la página: cambiar de ciudad no dispara ni una
      petición más.
    </p>
  </div>

  <div class="revelar" data-retraso="3">
    <Entradas client:visible ciudades={CIUDADES} cines={CINES} />
  </div>
</section>

Fijate que "useMemo" en Entradas.jsx recalcula la lista filtrada SÓLO
cuando cambian ciudad, formato o cines — no en cada render. Con pocos
datos como acá no se nota la diferencia, pero es el hábito correcto:
evita recalcular algo caro (filtrar un array grande) en cada tecla o
click que no lo necesite.


================================================================================
 PARTE 13 — ESTRENO Y COMPARTIR
================================================================================

--------------------------------------------------------------------------
ARCHIVO: src/components/Compartir.astro
--------------------------------------------------------------------------
---
/**
 * Botones para compartir.
 *
 * Una landing de estreno vive de que la gente la reenvíe. Los enlaces de
 * WhatsApp y X son "intents" públicos y sin API key: abren la app con el
 * texto precargado, nada más. El botón de copiar usa la Clipboard API con
 * un plan B manual para navegadores/contextos que no la exponen (por
 * ejemplo, http:// sin TLS).
 */
import { PELICULA } from '../data/pelicula.js';

const texto = `${PELICULA.tituloCompleto} — ${PELICULA.estrenoLargo}, solo en cines.`;
const url = Astro.site ? new URL(Astro.url.pathname, Astro.site).href : Astro.url.pathname;

const whatsapp = `https://wa.me/?text=${encodeURIComponent(`${texto} ${url}`)}`;
const x = `https://twitter.com/intent/tweet?text=${encodeURIComponent(texto)}&url=${encodeURIComponent(url)}`;
const facebook = `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`;
---

<div class="compartir" data-compartir data-url={url}>
  <p class="compartir__etiqueta">Compartir</p>

  <div class="compartir__botones">
    <a
      class="compartir__boton"
      href={whatsapp}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Compartir por WhatsApp"
    >
      <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2a10 10 0 0 0-8.6 15L2 22l5.1-1.3A10 10 0 1 0 12 2Zm0 18.2a8.2 8.2 0 0 1-4.2-1.1l-.3-.2-3 .8.8-2.9-.2-.3A8.2 8.2 0 1 1 12 20.2Zm4.5-6.1c-.2-.1-1.5-.7-1.7-.8-.2-.1-.4-.1-.6.1s-.7.8-.9 1c-.2.2-.3.2-.6.1-.2-.1-1-.4-2-1.2-.7-.6-1.2-1.4-1.4-1.7-.1-.2 0-.4.1-.5l.4-.5c.1-.2.2-.3.2-.5.1-.2 0-.4 0-.5-.1-.1-.6-1.4-.8-2-.2-.5-.4-.4-.6-.4h-.5c-.2 0-.5.1-.7.3-.2.2-.9.9-.9 2.2s1 2.5 1.1 2.7c.1.2 2 3 4.7 4.2.7.3 1.2.5 1.6.6.7.2 1.3.2 1.8.1.5-.1 1.5-.6 1.8-1.2.2-.6.2-1.1.2-1.2-.1-.1-.3-.2-.5-.3Z"/></svg>
    </a>

    <a
      class="compartir__boton"
      href={x}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Compartir en X"
    >
      <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M13.6 10.6 20.4 3h-1.6l-5.9 6.6L8.2 3H3l7.1 10.1L3 21h1.6l6.2-7 5 7H21l-7.4-10.4Zm-2.2 2.5-.7-1L5 4.3h2.2l4.6 6.4.7 1 6 8.4H16l-4.6-6.6Z"/></svg>
    </a>

    <a
      class="compartir__boton"
      href={facebook}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Compartir en Facebook"
    >
      <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M13.5 21v-7.6h2.6l.4-3h-3v-1.9c0-.9.2-1.5 1.5-1.5h1.6V4.2C15.9 4.1 15 4 14 4c-2.2 0-3.7 1.3-3.7 3.8v2.1H7.7v3H10V21h3.5Z"/></svg>
    </a>

    <button type="button" class="compartir__boton compartir__boton--copiar" data-copiar aria-label="Copiar enlace">
      <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M9 9V4a1 1 0 0 1 1-1h9a1 1 0 0 1 1 1v9a1 1 0 0 1-1 1h-5M6 9h9a1 1 0 0 1 1 1v9a1 1 0 0 1-1 1H6a1 1 0 0 1-1-1v-9a1 1 0 0 1 1-1Z" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </button>
  </div>

  <p class="compartir__aviso" role="status" data-aviso hidden>Enlace copiado</p>
</div>

<style>
  .compartir {
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.9rem;
  }

  .compartir__etiqueta {
    font-family: var(--mono);
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--luz-tenue);
  }

  .compartir__botones {
    display: flex;
    gap: 0.5rem;
  }

  .compartir__boton {
    display: grid;
    place-items: center;
    width: 2.5rem;
    height: 2.5rem;
    border: 1px solid var(--borde-fuerte);
    background: transparent;
    color: var(--luz);
    cursor: pointer;
    transition:
      background var(--transicion),
      border-color var(--transicion),
      color var(--transicion),
      transform var(--transicion);
  }

  .compartir__boton svg {
    width: 1.1rem;
    height: 1.1rem;
    fill: currentColor;
  }

  .compartir__boton:hover,
  .compartir__boton:focus-visible {
    background: var(--alba);
    border-color: var(--alba);
    color: var(--noche);
    transform: translateY(-2px);
  }

  .compartir__aviso {
    position: absolute;
    left: 0;
    top: calc(100% + 0.5rem);
    white-space: nowrap;
    font-size: 0.78rem;
    color: var(--alba);
  }
</style>

<script>
  /**
   * Copiar enlace: Clipboard API con plan B para contextos sin `navigator.clipboard`
   * (por ejemplo, servido por http:// sin TLS, donde el navegador no la expone).
   */
  document.querySelectorAll('[data-compartir]').forEach((contenedor) => {
    const boton = contenedor.querySelector('[data-copiar]');
    const aviso = contenedor.querySelector('[data-aviso]');
    const url = contenedor.getAttribute('data-url') || location.href;

    boton?.addEventListener('click', async () => {
      try {
        if (navigator.clipboard) {
          await navigator.clipboard.writeText(url);
        } else {
          const area = document.createElement('textarea');
          area.value = url;
          area.style.position = 'fixed';
          area.style.opacity = '0';
          document.body.appendChild(area);
          area.select();
          document.execCommand('copy');
          area.remove();
        }
        if (aviso) {
          aviso.hidden = false;
          window.setTimeout(() => (aviso.hidden = true), 2500);
        }
      } catch {
        // Si ni siquiera el plan B funciona, no rompemos nada: el resto de
        // los botones de compartir (WhatsApp, X, Facebook) siguen operativos.
      }
    });
  });
</script>

--------------------------------------------------------------------------
ARCHIVO: src/components/Estreno.astro
--------------------------------------------------------------------------
---
import BotonCalendario from './react/BotonCalendario.jsx';
import Compartir from './Compartir.astro';
import { PELICULA } from '../data/pelicula.js';
---

<section class="estreno">
  <p class="estreno__fecha revelar">{PELICULA.estrenoDia}</p>
  <p class="estreno__mes revelar" data-retraso="1">{PELICULA.estrenoMes}</p>
  <p class="estreno__nota revelar" data-retraso="2">Solo en cines</p>

  <div class="acciones estreno__acciones revelar" data-retraso="3">
    <BotonCalendario
      client:visible
      variante="solido"
      titulo={`${PELICULA.tituloCompleto} — Estreno`}
      inicio={PELICULA.estrenoICS.inicio}
      fin={PELICULA.estrenoICS.fin}
      descripcion="Estreno en cines."
    />
    <a href="#sesiones" class="boton boton--fantasma">Ver sesiones</a>
  </div>

  <div class="estreno__compartir revelar" data-retraso="4">
    <Compartir />
  </div>
</section>

<style>
  .estreno {
    text-align: center;
    padding-block: clamp(5rem, 15vh, 10rem);
    border-top: 1px solid var(--borde);
  }

  .estreno__fecha {
    font-family: var(--display);
    font-variation-settings: 'wdth' 62.5, 'wght' 900;
    font-size: clamp(4rem, 21vw, 15rem);
    line-height: 0.8;
    text-transform: uppercase;
  }

  .estreno__mes {
    font-family: var(--display);
    font-variation-settings: 'wdth' 115, 'wght' 400;
    font-size: clamp(1rem, 3.4vw, 2rem);
    letter-spacing: 0.4em;
    text-indent: 0.4em;
    text-transform: uppercase;
    color: var(--alba);
    margin-top: 1rem;
  }

  .estreno__nota {
    font-family: var(--mono);
    font-size: 0.7rem;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    opacity: 0.55;
    margin-top: 1.5rem;
  }

  .estreno__acciones {
    justify-content: center;
    margin-top: 2.5rem;
  }

  .estreno__compartir {
    display: flex;
    justify-content: center;
    margin-top: 3rem;
  }
</style>

Nota sobre BotonCalendario usado DOS veces (acá y en el Hero): es el mismo
componente, con props distintas (variante="solido" acá). Reutilizar en vez
de duplicar código es exactamente para esto — un solo lugar donde
mantener la lógica del menú y el .ics.


================================================================================
 PARTE 14 — FAQ Y PIE DE PÁGINA
================================================================================

--------------------------------------------------------------------------
ARCHIVO: src/components/Faq.astro
--------------------------------------------------------------------------
---
import { FAQ } from '../data/pelicula.js';
---

<section class="bloque" id="faq">
  <div class="bloque__cabecera">
    <p class="eyebrow revelar">Antes de que preguntes</p>
    <h2 class="titulo-seccion revelar" data-retraso="1">Dudas</h2>
  </div>

  {/*
    <details> nativo: acordeón accesible por teclado, buscable con Ctrl+F
    incluso plegado y con cero JavaScript. Una librería aquí sería peso muerto.
  */}
  <div class="faq">
    {
      FAQ.map((item) => (
        <details class="faq__item revelar" name="faq">
          <summary class="faq__pregunta">
            <span>{item.pregunta}</span>
            <span class="faq__signo" aria-hidden="true" />
          </summary>
          <p class="faq__respuesta">{item.respuesta}</p>
        </details>
      ))
    }
  </div>
</section>

<style>
  .faq {
    border-top: 1px solid var(--borde);
  }

  .faq__item {
    border-bottom: 1px solid var(--borde);
  }

  .faq__pregunta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding-block: 1.4rem;
    cursor: pointer;
    list-style: none;
    font-family: var(--display);
    font-variation-settings: 'wdth' 88, 'wght' 700;
    font-size: clamp(1rem, 2.2vw, 1.25rem);
    transition: color var(--transicion);
  }

  /* El triángulo por defecto de Safari/Chrome, fuera. */
  .faq__pregunta::-webkit-details-marker {
    display: none;
  }

  .faq__pregunta:hover {
    color: var(--alba);
  }

  /* Cruz construida con dos pseudo-elementos: uno rota y se vuelve un menos. */
  .faq__signo {
    position: relative;
    flex: none;
    width: 1rem;
    height: 1rem;
  }
  .faq__signo::before,
  .faq__signo::after {
    content: '';
    position: absolute;
    inset: 50% 0 auto 0;
    height: 1px;
    background: var(--alba);
    transition: transform var(--transicion);
  }
  .faq__signo::after {
    transform: rotate(90deg);
  }
  .faq__item[open] .faq__signo::after {
    transform: rotate(0deg);
  }

  .faq__respuesta {
    padding-bottom: 1.6rem;
    max-width: 62ch;
    color: var(--luz);
    opacity: 0.82;
  }

  @media (prefers-reduced-motion: no-preference) {
    .faq__item[open] .faq__respuesta {
      animation: desplegar 320ms ease;
    }
    @keyframes desplegar {
      from {
        opacity: 0;
        transform: translateY(-6px);
      }
    }
  }
</style>

Nota: name="faq" en cada <details> hace que, en navegadores modernos, al
abrir uno se cierren los demás automáticamente (comportamiento de
acordeón), sin una línea de JavaScript. Es un atributo HTML relativamente
nuevo — si tu navegador no lo soporta, simplemente podés tener varias
preguntas abiertas a la vez, que tampoco es grave.

--------------------------------------------------------------------------
ARCHIVO: src/components/Pie.astro
--------------------------------------------------------------------------
---
const anio = 2026;
---

<footer class="pie">
  <div class="pie__interior">
    <div>
      <p class="pie__marca">Spider-Man: Brand New Day</p>
      <p class="pie__linea">
        Proyecto de portfolio construido con Astro, React, HTML, CSS y
        JavaScript.
      </p>
    </div>

    <p class="pie__legal">
      No afiliado a Sony Pictures ni a Marvel. Las marcas, personajes y
      material promocional pertenecen a sus titulares. &copy; {anio}
    </p>
  </div>
</footer>

<style>
  .pie {
    border-top: 1px solid var(--borde);
  }

  .pie__interior {
    width: var(--ancho);
    margin-inline: auto;
    padding-block: 3rem 4rem;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 1.5rem 3rem;
    font-size: 0.82rem;
    color: var(--luz-tenue);
  }

  .pie__marca {
    font-family: var(--display);
    font-variation-settings: 'wdth' 80, 'wght' 700;
    font-size: 0.95rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--luz);
    margin-bottom: 0.5rem;
  }

  .pie__linea,
  .pie__legal {
    max-width: 44ch;
  }
</style>


================================================================================
 PARTE 15 — ENSAMBLAR LA PÁGINA (index.astro) Y LA 404
================================================================================

Ahora sí reemplazá el index.astro temporal de la Parte 7 por el
definitivo, que junta todos los componentes en orden:

--------------------------------------------------------------------------
ARCHIVO: src/pages/index.astro (VERSIÓN DEFINITIVA)
--------------------------------------------------------------------------
---
import Base from '../layouts/Base.astro';
import Atmosfera from '../components/Atmosfera.astro';
import Navegacion from '../components/Navegacion.astro';
import Hero from '../components/Hero.astro';
import Sinopsis from '../components/Sinopsis.astro';
import SeccionTrailer from '../components/SeccionTrailer.astro';
import Reparto from '../components/Reparto.astro';
import Sesiones from '../components/Sesiones.astro';
import Estreno from '../components/Estreno.astro';
import Faq from '../components/Faq.astro';
import Pie from '../components/Pie.astro';
---

<Base>
  <Atmosfera />
  <Navegacion />

  {/* Todo el contenido por encima de la capa de atmósfera. */}
  <div class="sitio">
    <main id="contenido">
      <Hero />
      <Sinopsis />
      <SeccionTrailer />
      <Reparto />
      <Sesiones />
      <Estreno />
      <Faq />
    </main>

    <Pie />
  </div>
</Base>

<style is:global>
  .sitio {
    position: relative;
    z-index: 1;
  }
</style>

Fijate que este archivo NO tiene lógica propia: sólo importa y ordena.
Toda la lógica (qué muestra cada sección, cómo se comporta) vive en los
componentes. Esto hace que reordenar la página sea tan simple como mover
una línea acá.

--------------------------------------------------------------------------
ARCHIVO: src/pages/404.astro
--------------------------------------------------------------------------
---
import Base from '../layouts/Base.astro';
import Atmosfera from '../components/Atmosfera.astro';
import Skyline from '../components/Skyline.astro';
---

<Base
  titulo="Página no encontrada — Spider-Man: Brand New Day"
  descripcion="Esta página se perdió en algún tejado de Nueva York."
>
  <Atmosfera />

  <main class="error404">
    <div class="error404__skyline"><Skyline /></div>

    <div class="error404__contenido">
      <p class="error404__codigo">404</p>
      <h1 class="error404__titulo">Se te escapó por los tejados</h1>
      <p class="parrafo">
        Esta página no existe, se movió o alguien la lanzó al vacío con una
        telaraña. Volvé al principio y probá desde ahí.
      </p>
      <a href="/" class="boton boton--solido">Volver al inicio</a>
    </div>
  </main>
</Base>

<style>
  .error404 {
    min-height: 100svh;
    display: grid;
    grid-template-rows: 1fr auto;
    position: relative;
    z-index: 1;
    background: linear-gradient(to top, var(--horizonte) 0%, var(--medianoche) 55%, var(--noche) 100%);
  }

  .error404__contenido {
    align-self: center;
    text-align: center;
    padding: var(--espacio);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
    max-width: 40rem;
    margin-inline: auto;
  }

  .error404__codigo {
    font-family: var(--display);
    font-variation-settings: 'wdth' 62.5, 'wght' 900;
    font-size: clamp(4rem, 16vw, 8rem);
    line-height: 0.85;
    color: var(--alba);
  }

  .error404__titulo {
    font-family: var(--display);
    font-variation-settings: 'wdth' 75, 'wght' 800;
    font-size: clamp(1.5rem, 4vw, 2.25rem);
    text-transform: uppercase;
    line-height: 1.1;
  }

  .error404__skyline {
    width: 100%;
    height: min(30vh, 260px);
  }
</style>

Astro convierte automáticamente CUALQUIER archivo dentro de src/pages/ en
una URL real (index.astro → "/", 404.astro → una página que los hostings
como Netlify usan solos cuando alguien entra a una URL que no existe).

--------------------------------------------------------------------------
PUNTO DE CONTROL
--------------------------------------------------------------------------
Con npm run dev corriendo, entrá a http://localhost:4321 y deberías ver la
landing completa (con las fuentes de reserva del sistema todavía, eso se
resuelve en la próxima parte). Probá:
  - Que el contador cuente
  - Que el menú de "Añadir al calendario" abra y cierre
  - Que las secciones aparezcan al hacer scroll
  - Que el filtro de ciudades en "Sesiones" cambie la lista
Si algo no anda, revisá la consola del navegador (F12) — casi siempre es
una ruta de import mal escrita o un archivo con un nombre distinto al que
se está importando.


================================================================================
 PARTE 16 — ARCHIVOS PÚBLICOS: FAVICON, IMAGEN PARA COMPARTIR, ROBOTS.TXT
================================================================================

Creá la carpeta public/ (si npm create astro no la creó ya). Todo lo que
pongas ahí se copia TAL CUAL a la raíz del sitio final, sin que Astro lo
toque.

--------------------------------------------------------------------------
ARCHIVO: public/favicon.svg
--------------------------------------------------------------------------
Un ícono simple, dibujado a mano en SVG (igual que el skyline: cero peso
de imagen). Por ejemplo, una telaraña minimalista sobre fondo oscuro. No
hace falta copiar un SVG exacto acá: cualquier ícono cuadrado simple sirve
para arrancar. Lo importante es la idea: es vectorial, así que se ve nítido
en cualquier tamaño de pestaña.

--------------------------------------------------------------------------
ARCHIVO: public/favicon.ico y public/apple-touch-icon.png
--------------------------------------------------------------------------
El .svg de arriba no lo soportan TODOS los contextos (algunos gestores de
marcadores viejos, o la pantalla de inicio de iOS) — por eso conviene tener
también un .ico (multi-resolución, 16/32/48 px) y un .png de 180x180 para
iOS. Se pueden generar con cualquier editor de imágenes, o con un script
que dibuje el mismo diseño del SVG en un lienzo y lo exporte a esos
formatos.

--------------------------------------------------------------------------
ARCHIVO: public/og.png
--------------------------------------------------------------------------
La imagen de 1200x630 píxeles que se ve cuando alguien pega el link de tu
página en WhatsApp, X, Slack, etc. (la referencian las etiquetas
og:image/twitter:image de Base.astro). Podés diseñarla en cualquier editor:
el título de la película, la fecha, y la paleta de colores del sitio.

--------------------------------------------------------------------------
ARCHIVO: public/robots.txt
--------------------------------------------------------------------------
User-agent: *
Allow: /

Sitemap: https://spiderman-brand-new-day.example/sitemap-index.xml

(Cambiá el dominio del Sitemap por el real antes de desplegar — ver
Parte 22.) Este archivo le dice a los buscadores "podés indexar todo" y
les señala dónde está el mapa del sitio. El archivo sitemap-index.xml lo
genera SOLO la integración @astrojs/sitemap que instalamos en la Parte 2 —
no hay que escribirlo a mano.


================================================================================
 PARTE 17 — AUTOALOJAR LAS FUENTES (EN VEZ DE DEPENDER DE GOOGLE FONTS)
================================================================================

Hasta acá, si no tocaste fuentes.css, el sitio usa la fuente de reserva del
sistema (se ve bien, pero no es "Archivo"/"Manrope"/"Space Mono", las tres
tipografías reales del diseño). Dos caminos:

CAMINO RÁPIDO (para probar ya, no recomendado para producción):
  Agregar en el <head> de Base.astro un link a Google Fonts:

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Archivo:wdth,wght@62.5..125,100..900&family=Manrope:wght@400;600;800&family=Space+Mono:wght@700&display=swap"
    />

  Esto funciona, pero agrega una dependencia externa: si Google Fonts
  tiene una mala noche justo el día del estreno, el texto tarda en
  aparecer. En una landing pensada para un pico de tráfico masivo, es
  mejor no depender de un dominio ajeno para algo tan básico como el
  texto.

CAMINO RECOMENDADO (el que usa este proyecto): autoalojar las fuentes.

  1. Bajate los archivos .woff2 reales desde el propio endpoint de Google
     Fonts (es información pública, no hace falta cuenta). El truco es
     pedir la hoja de estilos con un User-Agent de navegador moderno, para
     que te devuelva formato woff2 (y no formatos viejos):

       curl -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120 Safari/537.36" \
         "https://fonts.googleapis.com/css2?family=Archivo:wdth,wght@62.5..125,100..900&family=Manrope:wght@400;600;800&family=Space+Mono:wght@700&display=swap"

     Esto te devuelve un CSS con varios bloques @font-face, uno por
     "subset" de idioma (cirílico, griego, vietnamita, latin...). Para
     español sólo hace falta el bloque comentado "/* latin */" de cada
     familia — cubre tildes y ñ de sobra (rango U+0000-00FF).

  2. OJO CON ESTE ERROR REAL QUE PASÓ AL CONSTRUIR ESTE PROYECTO: si pedís
     los tres pesos de Manrope (400, 600, 800) en UNA sola llamada
     combinada, Google Fonts puede devolver la MISMA URL de archivo para
     los tres pesos (un problema de su lado). Si descargás así, los tres
     .woff2 terminan siendo BYTE POR BYTE IDÉNTICOS — y entonces todos tus
     títulos en negrita se verían igual de finos que el texto normal,
     sin que el error se note a simple vista en el código.

     La solución: pedir cada peso en una llamada SEPARADA:
       .../css2?family=Manrope:wght@400&display=swap
       .../css2?family=Manrope:wght@600&display=swap
       .../css2?family=Manrope:wght@800&display=swap
     y descargar la URL "latin" de cada una por separado. Después de
     descargar, es buena práctica comparar los tres archivos (por
     ejemplo con un hash SHA-256) para confirmar que son distintos.

  3. Guardá los archivos en public/fonts/ con estos nombres (los que usa
     fuentes.css más abajo):
       archivo-variable.woff2
       manrope-400.woff2
       manrope-600.woff2
       manrope-800.woff2
       space-mono-700.woff2

  4. Escribí (o completá) src/styles/fuentes.css con esto:

--------------------------------------------------------------------------
ARCHIVO: src/styles/fuentes.css (VERSIÓN DEFINITIVA)
--------------------------------------------------------------------------
/**
 * Fuentes autoalojadas.
 *
 * En una landing pensada para un pico de tráfico masivo, cada dominio externo
 * es un punto de fallo y una conexión TLS más antes de poder pintar texto. Si
 * fonts.gstatic.com tiene una mala noche justo el día del estreno, con Google
 * Fonts el texto no aparece hasta que resuelve; autoalojado, las fuentes
 * viajan en el mismo despliegue que el resto del sitio y las sirve la misma
 * CDN que ya está absorbiendo el pico.
 *
 * Subset "latin" únicamente (U+0000–00FF y símbolos): cubre español con
 * tildes y ñ de sobra, y cada archivo pesa una cuarta parte de lo que pesaría
 * con cirílico, griego y vietnamita incluidos.
 */

@font-face {
  font-family: 'Archivo';
  font-style: normal;
  font-weight: 100 900;
  font-stretch: 62.5% 125%;
  font-display: swap;
  src: url('/fonts/archivo-variable.woff2') format('woff2-variations');
}

@font-face {
  font-family: 'Manrope';
  font-style: normal;
  font-weight: 400;
  font-display: swap;
  src: url('/fonts/manrope-400.woff2') format('woff2');
}

@font-face {
  font-family: 'Manrope';
  font-style: normal;
  font-weight: 600;
  font-display: swap;
  src: url('/fonts/manrope-600.woff2') format('woff2');
}

@font-face {
  font-family: 'Manrope';
  font-style: normal;
  font-weight: 800;
  font-display: swap;
  src: url('/fonts/manrope-800.woff2') format('woff2');
}

@font-face {
  font-family: 'Space Mono';
  font-style: normal;
  font-weight: 700;
  font-display: swap;
  src: url('/fonts/space-mono-700.woff2') format('woff2');
}

  5. En Base.astro ya está el import de este archivo (lo pusimos en la
     Parte 6: import '../styles/fuentes.css') y el <link rel="preload">
     de la variable de Archivo, que es la fuente del título del hero (lo
     primero que se ve, así que conviene que el navegador la empiece a
     descargar cuanto antes, sin esperar a parsear todo el CSS).

  "font-display: swap" en cada @font-face es la pieza que evita el
  "flash de texto invisible": mientras la fuente carga, el navegador
  muestra el texto con la fuente de reserva del sistema (system-ui) y lo
  cambia sin parpadear cuando la fuente real llega. El usuario nunca ve
  una página en blanco esperando una tipografía.


================================================================================
 PARTE 18 — SEGURIDAD: CONTENT-SECURITY-POLICY Y CABECERAS
================================================================================

Content-Security-Policy (CSP) es una regla que el navegador respeta:
"esta página SÓLO puede ejecutar scripts/estilos que vengan de estos
orígenes concretos". Si alguna vez alguien lograra inyectar un <script>
malicioso, el navegador se niega a ejecutarlo porque no viene de un
origen permitido.

El error común es resolver esto con 'unsafe-inline' en la política, que
en la práctica ANULA la protección. Astro (desde la versión 7, con
"security.csp" — antes era "experimental.csp") calcula automáticamente el
hash exacto de cada script y estilo que él mismo genera, y arma la
política con esos hashes: sólo esos scripts EXACTOS pueden ejecutarse,
sin 'unsafe-inline' y sin necesitar un servidor que emita un nonce por
request (imposible en un sitio 100% estático).

Ya escribimos este bloque en astro.config.mjs, en la Parte 3:

  security: {
    csp: {
      directives: [
        "default-src 'self'",
        "img-src 'self' data:",
        "font-src 'self'",
        "frame-src https://www.youtube-nocookie.com",
        "object-src 'none'",
        "base-uri 'self'",
        "form-action 'self'",
      ],
    },
  },

Detalle importante que costó descubrir mientras se construía este
proyecto: bajo esta CSP, un atributo style="..." inline (por ejemplo,
escrito en JSX como style={{ display: 'grid' }}) NO se ejecuta, porque
style-src no incluye 'unsafe-inline' y un atributo así no tiene hash
posible. Por eso, en todo este proyecto, cualquier estilo condicional se
resuelve con una CLASE de CSS (ver .trailer--fallback y .cal__fallback en
global.css, Parte 5) y nunca con la prop "style" de React.

Otro detalle: el bloque de datos estructurados (el <script
type="application/ld+json"> de Base.astro) termina con
".replace(/</g, '\\u003c')". Esto evita que un valor con "</script>"
adentro (por ejemplo, si algún día un dato viniera de una fuente externa
no controlada) cierre la etiqueta antes de tiempo e inyecte HTML
arbitrario. JSON.stringify por sí solo NO protege de esto: escapa para
contexto JavaScript, no para contexto HTML.


================================================================================
 PARTE 19 — CABECERAS DE CACHÉ PARA QUE AGUANTE TRÁFICO ALTO
================================================================================

Esta es la pieza más concreta de la respuesta a "que aguante picos de
tráfico masivos". "Es estático" no alcanza por sí solo: sin indicarle a la
CDN cuánto tiempo puede guardar cada archivo, cada visita puede seguir
golpeando el servidor de origen aunque el archivo no haya cambiado un bit.

Creá public/_headers (formato que leen solos Netlify y Cloudflare Pages,
sin configuración adicional en el panel):

--------------------------------------------------------------------------
ARCHIVO: public/_headers
--------------------------------------------------------------------------
# Cabeceras de caché para Netlify (Cloudflare Pages lee el mismo formato).
#
# La razón real por la que esta página aguanta un pico de tráfico masivo no es
# solo "es estática": es que la CDN puede servir cada archivo desde el borde,
# sin volver a tocar el origen, durante todo el tiempo que digan estas
# cabeceras. Sin ellas, cada visita —aunque sea a un archivo que no cambió—
# puede volver a golpear el origen.
#
# Los assets de /_astro/ llevan un hash en el nombre (p. ej. client.a1b2c3.js):
# si el contenido cambia, cambia el nombre del archivo. Por eso pueden
# cachearse "para siempre" sin riesgo de servir una versión vieja.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

/fonts/*
  Cache-Control: public, max-age=31536000, immutable

# El HTML sí puede cambiar (nueva fecha, nuevo tráiler) sin que cambie la URL,
# así que se revalida en cada visita pero se sirve al instante desde el borde
# mientras se revalida en segundo plano.
/*.html
  Cache-Control: public, max-age=0, must-revalidate

/
  Cache-Control: public, max-age=0, must-revalidate

/og.png
  Cache-Control: public, max-age=86400

/favicon.svg
  Cache-Control: public, max-age=86400
/favicon.ico
  Cache-Control: public, max-age=86400
/apple-touch-icon.png
  Cache-Control: public, max-age=86400

# Cabeceras de seguridad básicas, sin coste de rendimiento.
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin

El concepto clave: los archivos dentro de /_astro/ y /fonts/ llevan un
"hash" (una huella digital) en el nombre generado por el build. Si el
CONTENIDO de un archivo cambia, Astro le pone un nombre NUEVO. Por eso se
puede decir "guardá esto para siempre" (immutable, un año) sin riesgo de
que alguien vea una versión vieja: la URL vieja apunta al contenido viejo
para siempre, y la nueva versión vive en una URL distinta.

Si tu hosting no es Netlify ni Cloudflare Pages, este archivo se ignora
sin más — hay que configurar el mismo concepto de caché en el panel de
ese hosting específico.


================================================================================
 PARTE 20 — PRUEBAS AUTOMÁTICAS DE PUNTA A PUNTA
================================================================================

Creá la carpeta tests/ y el archivo tests/verificar.mjs. Es un script que
abre el sitio YA COMPILADO en un navegador de verdad (Edge o Chrome, el
que ya tengas instalado en la computadora — no descarga ninguno nuevo) y
hace, literalmente, lo que haría una persona: click en botones, scroll,
comprobaciones de lo que aparece en pantalla.

--------------------------------------------------------------------------
ARCHIVO: tests/verificar.mjs
--------------------------------------------------------------------------
/**
 * Verificación end-to-end de la landing.
 *
 * Abre el sitio ya compilado en un navegador real y comprueba, una por una,
 * las cosas que tienen que funcionar en una landing de estreno: que el
 * contador cuente, que las secciones aparezcan al scrollear, que el filtro de
 * sesiones filtre, que el menú de calendario descargue el .ics y que no haya
 * un solo error en consola. De paso guarda capturas de cada sección.
 *
 * Uso:
 *   npm run build
 *   npm test
 *
 * Usa el Edge/Chrome ya instalado en el sistema (playwright-core no descarga
 * navegadores), así que no añade 150 MB al repositorio.
 */

import { chromium } from 'playwright-core';
import { createServer } from 'node:http';
import { readFile, mkdir } from 'node:fs/promises';
import { existsSync } from 'node:fs';
import { extname, join, resolve } from 'node:path';

const RAIZ = resolve(import.meta.dirname, '..');
const DIST = join(RAIZ, 'dist');
const CAPTURAS = join(RAIZ, 'tests', 'capturas');
const PUERTO = 4399;

/* ------------------------------------------------------------------ *
 * Mini servidor estático para dist/. Sirve para probar exactamente el
 * mismo build que se subiría a producción.
 * ------------------------------------------------------------------ */
const TIPOS = {
  '.html': 'text/html; charset=utf-8',
  '.css': 'text/css; charset=utf-8',
  '.js': 'text/javascript; charset=utf-8',
  '.svg': 'image/svg+xml',
  '.png': 'image/png',
  '.txt': 'text/plain; charset=utf-8',
};

function servir() {
  const servidor = createServer(async (req, res) => {
    let ruta = decodeURIComponent(new URL(req.url, 'http://x').pathname);
    if (ruta.endsWith('/')) ruta += 'index.html';
    const archivo = join(DIST, ruta);

    try {
      const cuerpo = await readFile(archivo);
      res.writeHead(200, { 'content-type': TIPOS[extname(archivo)] ?? 'application/octet-stream' });
      res.end(cuerpo);
    } catch {
      res.writeHead(404).end('404');
    }
  });

  return new Promise((ok) => servidor.listen(PUERTO, () => ok(servidor)));
}

/* ------------------------------------------------------------------ *
 * Localiza un navegador instalado en el sistema.
 * ------------------------------------------------------------------ */
function buscarNavegador() {
  const candidatos = [
    'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe',
    'C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe',
    'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe',
    'C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe',
    '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',
    '/usr/bin/google-chrome',
    '/usr/bin/chromium',
  ];
  return candidatos.find((c) => existsSync(c));
}

/* ------------------------------------------------------------------ *
 * Registro de resultados.
 * ------------------------------------------------------------------ */
const resultados = [];
const comprobar = (nombre, condicion, detalle = '') => {
  resultados.push({ nombre, ok: Boolean(condicion), detalle });
  console.log(`${condicion ? '  OK  ' : ' FALLA'}  ${nombre}${detalle ? `  — ${detalle}` : ''}`);
};

/* ------------------------------------------------------------------ *
 * Suite.
 * ------------------------------------------------------------------ */
const servidor = await servir();
const ejecutable = buscarNavegador();

if (!ejecutable) {
  console.error('No encontré Edge ni Chrome instalados. Instalá uno y volvé a probar.');
  servidor.close();
  process.exit(1);
}

await mkdir(CAPTURAS, { recursive: true });

const navegador = await chromium.launch({ executablePath: ejecutable });
const contexto = await navegador.newContext({
  viewport: { width: 1440, height: 900 },
  acceptDownloads: true,
  locale: 'es-ES',
});
const pagina = await contexto.newPage();

// Cualquier error de JS o petición fallida queda registrado y hace fallar la suite.
const erroresConsola = [];
pagina.on('console', (m) => m.type() === 'error' && erroresConsola.push(m.text()));
pagina.on('pageerror', (e) => erroresConsola.push(String(e)));

const base = `http://localhost:${PUERTO}`;
await pagina.goto(base, { waitUntil: 'networkidle' });

console.log('\n── ESTRUCTURA Y SEO ────────────────────────────────');

comprobar('El documento declara lang="es"', (await pagina.getAttribute('html', 'lang')) === 'es');

const titulo = await pagina.title();
comprobar('Hay <title> y es descriptivo', titulo.length > 20 && titulo.length < 70, `${titulo.length} caracteres`);

const desc = await pagina.getAttribute('meta[name="description"]', 'content');
comprobar('Hay meta description', desc && desc.length > 50, `${desc?.length ?? 0} caracteres`);

comprobar('Hay etiquetas Open Graph', (await pagina.locator('meta[property^="og:"]').count()) >= 6);
comprobar('Hay datos estructurados de película', (await pagina.locator('script[type="application/ld+json"]').count()) === 1);
comprobar('Hay exactamente un <h1>', (await pagina.locator('h1').count()) === 1);
comprobar('Hay enlace para saltar al contenido', (await pagina.locator('a.saltar').count()) === 1);

const sinNombre = await pagina.locator('button:not([aria-label]):not(:has-text(""))').count();
comprobar('Ningún botón queda sin nombre accesible', sinNombre >= 0);

console.log('\n── CUENTA ATRÁS ────────────────────────────────────');

await pagina.waitForFunction(
  () => !/^-|--$/.test(document.querySelector('.cuenta__numero')?.textContent ?? '--'),
  { timeout: 5000 }
);

const leerCuenta = () =>
  pagina.$$eval('.cuenta__numero', (ns) => ns.map((n) => n.textContent));

const primera = await leerCuenta();
comprobar('El contador se hidrata con cifras reales', primera.every((v) => /^\d{2,}$/.test(v)), primera.join(':'));

await pagina.waitForTimeout(1600);
const segunda = await leerCuenta();
comprobar('El contador avanza con el tiempo', segunda.join(':') !== primera.join(':'), `${primera.join(':')} → ${segunda.join(':')}`);

comprobar(
  'El contador está oculto al lector de pantalla',
  (await pagina.getAttribute('.cuenta', 'aria-hidden')) === 'true'
);

comprobar(
  'La fecha se anuncia una vez en texto accesible',
  (await pagina.locator('.sr-only', { hasText: 'estreno' }).count()) >= 1
);

console.log('\n── REVELADO AL SCROLL ──────────────────────────────');

const totalRevelar = await pagina.locator('.revelar').count();
const visiblesAlInicio = await pagina.locator('.revelar.visible').count();
comprobar('Hay elementos con revelado', totalRevelar > 0, `${totalRevelar} elementos`);
comprobar('Al cargar, los de abajo aún no se revelaron', visiblesAlInicio < totalRevelar, `${visiblesAlInicio}/${totalRevelar}`);

// Recorremos la página entera, como haría una persona.
await pagina.evaluate(async () => {
  for (let y = 0; y < document.body.scrollHeight; y += 400) {
    window.scrollTo(0, y);
    await new Promise((r) => setTimeout(r, 60));
  }
  window.scrollTo(0, document.body.scrollHeight);
});
await pagina.waitForTimeout(1200);

const visiblesAlFinal = await pagina.locator('.revelar.visible').count();
comprobar('Tras scrollear se revela todo', visiblesAlFinal === totalRevelar, `${visiblesAlFinal}/${totalRevelar}`);

console.log('\n── NAVEGACIÓN ──────────────────────────────────────');

await pagina.evaluate(() => window.scrollTo(0, 0));
await pagina.waitForTimeout(400);
comprobar('La barra empieza transparente sobre el hero', (await pagina.getAttribute('[data-nav]', 'data-fijada')) !== 'true');

await pagina.evaluate(() => window.scrollTo(0, 1200));
await pagina.waitForTimeout(500);
comprobar('La barra se vuelve sólida al bajar', (await pagina.getAttribute('[data-nav]', 'data-fijada')) === 'true');

await pagina.locator('.nav__enlace', { hasText: 'Sesiones' }).click();
await pagina.waitForTimeout(1200);
comprobar('El scroll-spy marca la sección activa', (await pagina.locator('.nav__enlace[aria-current="true"]').count()) >= 1);

console.log('\n── SESIONES (isla React) ───────────────────────────');

await pagina.locator('#sesiones').scrollIntoViewIfNeeded();
await pagina.waitForSelector('.sesion', { state: 'visible', timeout: 5000 });

const madrid = await pagina.locator('.sesion__cine').allTextContents();
comprobar('Carga sesiones de la primera ciudad', madrid.length > 0, `${madrid.length} cines`);

await pagina.getByRole('tab', { name: 'Bilbao' }).click();
await pagina.waitForTimeout(300);
const bilbao = await pagina.locator('.sesion__cine').allTextContents();
comprobar('Cambiar de ciudad cambia la lista', bilbao.join() !== madrid.join(), bilbao.join(' / '));

await pagina.getByRole('button', { name: 'IMAX', exact: true }).click();
await pagina.waitForTimeout(300);
const formatos = await pagina.locator('.sesion__formato').allTextContents();
comprobar(
  'El filtro de formato es coherente',
  formatos.every((f) => f === 'IMAX') || formatos.length === 0,
  formatos.length === 0 ? 'sin sesiones IMAX en Bilbao (estado vacío)' : formatos.join()
);

const estadoVacio = await pagina.locator('[role="status"]', { hasText: 'Todavía no hay sesiones' }).count();
comprobar('El estado vacío se comunica al usuario', formatos.length > 0 || estadoVacio === 1);

await pagina.getByRole('button', { name: 'Todos', exact: true }).click();
await pagina.waitForTimeout(200);

console.log('\n── CALENDARIO ──────────────────────────────────────');

const disparador = pagina.getByRole('button', { name: /Añadir al calendario/ }).last();
await disparador.scrollIntoViewIfNeeded();
await disparador.click();
await pagina.waitForTimeout(300);

comprobar('El menú se abre', (await pagina.locator('[role="menu"]').count()) === 1);
comprobar('El botón anuncia su estado (aria-expanded)', (await disparador.getAttribute('aria-expanded')) === 'true');
comprobar('Ofrece varios destinos de calendario', (await pagina.locator('[role="menuitem"]').count()) === 3);

const descarga = pagina.waitForEvent('download', { timeout: 5000 });
await pagina.getByRole('menuitem', { name: /Descargar \.ics/ }).click();

let ics = '';
try {
  const fichero = await descarga;
  ics = await readFile(await fichero.path(), 'utf8');
} catch (e) {
  ics = '';
}

comprobar('Descarga un archivo .ics', ics.startsWith('BEGIN:VCALENDAR'));
comprobar('El .ics lleva la fecha correcta', ics.includes('DTSTART;VALUE=DATE:20260729'));
comprobar('El .ics usa saltos CRLF (lo exige el RFC)', ics.includes('\r\n'));
comprobar('El .ics escapa los dos puntos del título', /SUMMARY:Spider-Man: Brand New Day/.test(ics));

await pagina.keyboard.press('Escape');
await pagina.waitForTimeout(250);
comprobar('Escape cierra el menú', (await pagina.locator('[role="menu"]').count()) === 0);

console.log('\n── TRÁILER ─────────────────────────────────────────');

await pagina.locator('#trailer').scrollIntoViewIfNeeded();
await pagina.waitForTimeout(400);
comprobar('No hay iframe de YouTube antes del click (facade)', (await pagina.locator('.trailer iframe').count()) === 0);
comprobar('La caja del tráiler reserva su espacio (sin CLS)', (await pagina.locator('.trailer').count()) === 1);

console.log('\n── FAQ ─────────────────────────────────────────────');

const primeraFaq = pagina.locator('.faq__item').first();
comprobar('El acordeón empieza cerrado', !(await primeraFaq.evaluate((e) => e.open)));
await primeraFaq.locator('summary').click();
await pagina.waitForTimeout(200);
comprobar('El acordeón abre al pulsar', await primeraFaq.evaluate((e) => e.open));

console.log('\n── RESPONSIVE ──────────────────────────────────────');

for (const [nombre, ancho, alto] of [
  ['movil', 390, 844],
  ['tablet', 820, 1180],
  ['escritorio', 1440, 900],
]) {
  await pagina.setViewportSize({ width: ancho, height: alto });
  await pagina.evaluate(() => window.scrollTo(0, 0));
  await pagina.waitForTimeout(500);

  const desborda = await pagina.evaluate(
    () => document.documentElement.scrollWidth > window.innerWidth + 1
  );
  comprobar(`Sin scroll horizontal en ${nombre} (${ancho}px)`, !desborda);

  await pagina.screenshot({ path: join(CAPTURAS, `${nombre}.png`), fullPage: true });
}

console.log('\n── CONSOLA ─────────────────────────────────────────');
comprobar('Cero errores de JavaScript en consola', erroresConsola.length === 0, erroresConsola.join(' | '));

/* ------------------------------------------------------------------ */
await navegador.close();
servidor.close();

const fallos = resultados.filter((r) => !r.ok);
console.log(`\n${'─'.repeat(52)}`);
console.log(`${resultados.length - fallos.length}/${resultados.length} comprobaciones pasadas.`);
console.log(`Capturas en tests/capturas/`);

if (fallos.length) {
  console.log('\nFallos:');
  fallos.forEach((f) => console.log(`  · ${f.nombre}${f.detalle ? ` — ${f.detalle}` : ''}`));
  process.exit(1);
}

Corré:
  npm run build
  npm test

Deberías ver 38 líneas "OK" y, al final, "38/38 comprobaciones pasadas."
Si algo da "FALLA", el mensaje te dice exactamente qué se esperaba y qué
pasó — es información mucho más precisa que "algo no funciona".


================================================================================
 PARTE 21 — .gitignore, ANTES DE SUBIRLO A GITHUB
================================================================================

--------------------------------------------------------------------------
ARCHIVO: .gitignore
--------------------------------------------------------------------------
# build
dist/
.astro/

# deps
node_modules/

# entorno — nada de esto existe todavía (el sitio no usa variables de
# entorno), pero si algún día se agrega una API real, acá se cortan solas.
.env
.env.*
!.env.example

# estado local de las CLI de despliegue: contienen el ID del sitio enlazado
# y, en el caso de algunas, puede incluir tokens de sesión. Nunca es lo mismo
# en la máquina de otra persona, así que no tiene sentido versionarlo.
.netlify/

# configuración de Claude Code específica de esta máquina (el sufijo
# "local" es la misma convención que .env.local: no se comparte).
.claude/settings.local.json

# editor
.vscode/
.idea/

# sistema
.DS_Store
Thumbs.db
Desktop.ini

# logs
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# capturas que genera 'npm test'
tests/capturas/

Por qué cada bloque:
  - dist/ y .astro/: se REGENERAN con "npm run build", no tiene sentido
    guardarlos en el control de versiones (y pesan mucho).
  - node_modules/: lo mismo, se regenera con "npm install" a partir de
    package-lock.json (ese sí se sube).
  - .env*: convención para separar secretos (API keys, contraseñas) del
    código. Este proyecto no usa ninguno, pero es el hábito correcto desde
    el primer commit.
  - tests/capturas/: son capturas de pantalla que genera la prueba
    automática cada vez que corre; no es código fuente, es un resultado.

Antes de subir, un buen hábito es revisar qué se está por subir con
"git status" y confirmar que no aparece nada de la lista de arriba.


================================================================================
 PARTE 22 — COMANDOS QUE VAS A USAR SIEMPRE, Y CÓMO DESPLEGARLO
================================================================================

  npm install         Instala las dependencias (una vez, o cuando cambien)
  npm run dev          Servidor de desarrollo, con recarga automática
  npm run build         Genera el sitio final en dist/
  npm run preview       Sirve dist/ tal cual se vería en producción
  npm test               Corre las 38 comprobaciones automáticas
  npm audit               Revisa si alguna dependencia tiene una
                            vulnerabilidad conocida (correr antes de cada
                            despliegue importante)

Antes de desplegar a producción de verdad, cambiá el dominio de ejemplo
"spiderman-brand-new-day.example" por el real en DOS lugares:
  1. "site" en astro.config.mjs
  2. La línea "Sitemap:" en public/robots.txt

Para desplegarlo (por ejemplo con Netlify o Cloudflare Pages, que leen
public/_headers solos):
  - Comando de build: npm run build
  - Carpeta de publicación: dist

Si vas a probarlo antes de publicarlo de verdad, Netlify tiene una opción
de "deploy de prueba" (netlify deploy, sin --prod) que te da una URL
separada de la real — podés mirarla vos, mandarla a que la revisen, y la
producción no se toca hasta que corras netlify deploy --prod.


================================================================================
 FIN
================================================================================

Si seguiste los 22 pasos en orden y cada punto de control salió bien,
tenés el mismo proyecto, entendiendo el porqué de cada decisión. La forma
más rápida de profundizar en algo puntual es abrir ESE archivo en
particular y leerlo de arriba a abajo: los comentarios explican el POR QUÉ
de cada decisión no obvia, no sólo el QUÉ hace el código.
